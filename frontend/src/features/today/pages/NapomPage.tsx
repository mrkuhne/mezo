// A napom — `/nap/napom` + `/nap/napom/:date` (mezo-yjzhw.4). Stub: Task 6 replaces this
// with the real page (reading, next action, morning mode) built on Task 3/4's hooks.
export function NapomPage() {
  return null
}
