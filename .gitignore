# Dependencies
node_modules/
.pnp/
.pnp.js

# Claude Code local worktrees (harness-managed, never commit)
.claude/worktrees/
# Local browser-preview dev-server config — machine-specific, never shared.
.claude/launch.json
# Impeccable design skill — vendored payload (~5 MB, 164 files), installed per machine
# with `npx impeccable install` and refreshed with `npx impeccable update`.
# Playbook: docs/infrastructure/impeccable-frontend-review.md
.claude/skills/impeccable/
.claude/agents/impeccable-*.md
.impeccable/config.local.json
.impeccable/live/

# Build output
dist/
build/
out/
target/
*.tsbuildinfo

# tsc -b composite emit artifacts (regenerated from *.ts configs)
vite.config.js
vite.config.d.ts
**/tests/visual/playwright.config.js
**/tests/visual/playwright.config.d.ts

# Environment & secrets
.env
.env.*
!.env.example
*.pem

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp

# Coverage / test artifacts
coverage/
.nyc_output/

# Playwright visual harness — run artifacts only
# (the self-baseline PNGs under tests/visual/visual.spec.ts-snapshots/ ARE committed)
frontend/tests/visual/test-results/
test-results/
playwright-report/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Superpowers brainstorm visual-companion (local only)
.superpowers/
.playwright-mcp/

# Manual git worktrees for independent sessions (never commit)
.worktrees/
issues.jsonl

# Frontend visual-parity harness shots + ad-hoc QA scratch (not part of the backend history; mezo-xixu cleanup)
frontend/tests/parity/
/p7-*.png
/p7-*.yml
/vr-qa-*.jpeg
