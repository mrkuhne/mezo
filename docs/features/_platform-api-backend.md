---
title: Platform · API Contract & Backend Architecture
type: feature-platform
status: done
updated: 2026-09-03
tags: [platform, backend, data-layer, frontend]
key_files:
  - api/openapi.yml
  - api/generate
  - backend/src/main/java/io/mrkuhne/mezo
  - backend/pom.xml
  - backend/src/main/resources/messages.properties
  - frontend/src/data/_client/api.ts
  - frontend/src/data/fuel/pantryApi.ts
  - frontend/src/data/hooks.ts
related: [_platform-data-layer, _platform-auth-security, _platform-notifications, train, today, fuel, ritual, companion]
---

# Platform · API Contract & Backend Architecture — Feature Documentation

> One-line: the contract-first OpenAPI pipeline (`api/`) + the Spring Boot 4 backend spine (`backend/`) + the frontend consumption seam (`frontend/src/data/*`, `frontend/src/data/hooks.ts`). **Status ✅ done as Phase-2 infrastructure** for auth · biometrics · goal · Train · Fuel (all sub-slices) · People · companion; the durable backbone every backed feature flows through — **Phase 2 closed 2026-07-05**. Not a route/tab — it underlies all of them.

This is the platform doc: read it before, or alongside, any per-feature backend doc (auth, biometrics, goal, Train, Fuel). **Phase 2 is CLOSED (exit audit `mezo-t16y.4`, 2026-07-05):** every real-mode surface is backend-backed, composed from real reads, or an explicit honest state — Fuel (Pantry+Import/Recipes/Meal+Scoring/Medication/Stack/Water/Week), People, Today, Insights-Weekly all ride this spine; the deliberate leftovers (Replan/Stack-rec fixtures) are 🟣 Fuel-P8 material — **the proactive epic is COMPLETE** (briefing + weekly + memoir + heartbeat + predictions + experiments all shipped; only H2 Web Push infra deferred); Phase-3 AI shipped separately (see `companion.md`).

---

## 1. Summary

The FE↔BE boundary is defined **once**, in OpenAPI, under `api/`. Per-feature fragments (`api/feature/<x>/<x>.yml`) are merged into a single committed `api/openapi.yml`, which is the source of truth for **both** generators:

- the backend `openapi-generator-maven-plugin` (`spring` generator) emits controller interfaces (`io.mrkuhne.mezo.api.controller.<Tag>Api`) + request/response DTOs (`io.mrkuhne.mezo.api.dto.*`) at `generate-sources`;
- the frontend `openapi-typescript` emits `frontend/src/data/_client/api.gen.ts` (types only).

Drift between the two sides becomes a **compile error**, not a runtime surprise. On the backend each feature follows a fixed `feature/<name>/{controller,service,repository,entity,mapper}` layout on top of reusable `techcore/` building blocks: UUID-PK soft-deleted *owned* entities, multi-user ownership resolved from the JWT principal, a `SystemMessage`-based error contract, and an integration-first test framework.

**Status per layer:**

| Domain | Contract fragment | Backend | FE wiring | Status |
|---|---|---|---|---|
| **Auth** | `api/feature/auth/auth.yml` | ✅ `feature/auth` | `data/auth/authApi.ts` + `app/auth/AuthGate.tsx` | ✅ Real. Multi-user (`mezo-qw37`) invite-gated register/login, JWT HS256. |
| **Biometrics — weight** | `api/feature/weight/weight.yml` | ✅ `feature/biometrics/weight` | `data/me/biometricsApi.ts` → `useWeight()` | ✅ Real, dual-mode. G5 (`mezo-g1u`) adds the live `GET /api/biometrics/weight/trend` (EWMA series + rate + sufficiency) via `WeightTrendService`; FE swap of the `weightTrends` mock is G5 Task 11. |
| **Biometrics — sleep** | `api/feature/sleep/sleep.yml` | ✅ `feature/biometrics/sleep` | `data/me/biometricsApi.ts` → `useSleep()` | ✅ Real, dual-mode. **`mezo-dbsr` enriched `sleep_log`** additively (nullable in-bed + phase minutes + `source`) for slice B — **now filled by the screenshot-confirm path (`mezo-66ab`)**; the log POST widened accordingly. |
| **Biometrics — sleep goal** (day-anchor) | `api/feature/sleep-goal/sleep-goal.yml` | ✅ `feature/biometrics/sleep` (`SleepGoalService` + **ungated `SleepAnchorResolver`**) | `data/me/sleepHooks.ts` → `useSleepGoal()` / `useSleepGoalActions()` | ✅ Real, dual-mode (`mezo-dbsr`). Per-user singleton `sleep_goal` (duration target + fixed `WAKE`/`BED` anchor → the other end derived); `GET/PUT /api/sleep/goal` (tag `SleepGoal` → `SleepGoalApi`), gated on `mezo.feature.sleep-goal.enabled`; **GET never 404s** — config-ghost from `SleepGoalProperties` (`mezo.sleep`). The **ungated `SleepAnchorPort`** is the single wake/bed source Habit + Fuel resolve through (works even with the surface off). See `me.md` §4. |
| **Biometrics — sleep screenshot** (SleepShot) | `api/feature/sleep-shot/sleep-shot.yml` | ✅ `feature/biometrics/sleep` (`SleepShotService` + deterministic `SleepShotDraftValidator` + the sleep-owned `SleepShotLlm` port) | `data/me/sleepHooks.ts` → `useSleepShot()` (action-only, no query key) | ✅ Real, dual-mode (`mezo-66ab`, slice B). `POST /api/sleep/screenshot` (tag `SleepShot` → `SleepShotApi`), gated on `mezo.feature.sleep-shot.enabled`; **multipart** photo (jpeg/png/webp ≤5 MB — `mezo.sleep-shot` → `SleepShotProperties`) → **ONE** vision call → an editable `SleepShotDraftResponse` whose **confidence/needsReview are deterministic** (the validator, never the LLM). **Nothing persists** — the FE confirms the draft via `POST /api/biometrics/sleep` (`source:'screenshot'`). Errors: 400 photo / 502 `SLEEP_SHOT_EXTRACT_FAILED` / 503 `SLEEP_SHOT_LLM_UNAVAILABLE` (companion off). Reaches the model through the companion vision LLM via the sleep-owned port ([ADR 0012](../decisions/0012-consumer-owned-llm-ports.md)). See `me.md` §3–§4 / `companion.md` §5.3. |
| **Biometrics — check-in** | `api/feature/checkin/checkin.yml` | ✅ `feature/biometrics/checkin` | `data/me/biometricsApi.ts` → `useCheckins()` | ✅ Real read + write (Slice T `mezo-t16y.3`: `listForDay` hydrates the strip; save invalidates the day query). |
| **Biometrics — profile** | `api/feature/biometrics-profile/biometrics-profile.yml` | ✅ `feature/biometrics/profile` | `data/me/biometricProfileApi.ts` → `useBiometricProfile()` + `useBiometricActions()` (G6) | ✅ Real, dual-mode (G1, `mezo-2hp`; **wired G6 `mezo-06n`**). **G6**: the GET now carries a **derived** `tdeeBootstrap` (base-TDEE from the profile + latest weigh-in via `TdeeBootstrapService.compute`; cross-`$ref` to the goal fragment's `TdeeBootstrap`; NOT persisted, null with no weigh-in), and `BiometricProfileService.upsertProfile` **recomputes the owner's active goal** (the biometric-profile-change engine trigger, the engine's **5th recompute trigger** — see [`goal-engine.md`](goal-engine.md) §3). **GET never 404s (mezo-5cmq)** — no profile row → 200 with every field null (Jackson's default inclusion, not `{}`); the pre-5cmq 404 shape is still tolerated on the FE (the two deployed images don't switch at once). FE: the Profil "Biometria" card + `BiometricSheet` editor consume the two hooks (read empty/old-404→null, base-TDEE display; write invalidates `['biometricProfile']`+`['goals']`), and a complete profile is the hard gate on goal creation. |
| **Goal** (`Cél`) | `api/feature/goal/goal.yml` | ✅ `feature/goal` | `data/me/goalApi.ts` + `data/me/goalLinkApi.ts` → `useGoal()` (read) + `useGoalActions()` (G4b mgmt) + `useGoalCreation()` (G4a write) | ✅ Real (G1 `mezo-2hp` + G3 `mezo-3sc` + G4a `mezo-pqt` + G4b `mezo-tji`). CRUD + activate/archive lifecycle; G3 adds the `goal_plan_link` aggregate (timeline + attach/detach). FE reads active goal + real linked plans; **G4a wires the first write** — `useGoalCreation` POSTs a new goal (+ `biometric_profile` upsert + optional activate) via the `GoalPlannerPage` wizard; **G4b wires the command-center management** — `useGoalActions` drives archive/delete + plan attach/detach (FE-only over the existing endpoints), and adds the goal-upsert **inverted-window guard** in `GoalService` (see §4d). **G5 (`mezo-g1u`) lands the TDEE/prescription engine + its live HTTP surface** — `POST /api/goals/{id}/evaluate` (`GoalController` → `GoalEngineService.evaluate`) assembles + persists the segmented `prescription` + `tdeeBootstrap` jsonb onto the goal and re-fetches via `getGoal` (the `GoalMapper` projects both into `GoalResponse`); the no-profile case returns **200 with a graceful feasibility note** (never a 4xx, so the recompute triggers don't break), a foreign/missing goal still 404s. **G6 (`mezo-06n`)** derives the goal's target pace server-side: `GoalService.applyUpsert` computes `rateTargetPctPerWeek` from the target weight+date, so it was **dropped from `GoalUpsertRequest`** (still on `GoalResponse`); a new stateless `POST /api/goals/feasibility-preview` (`GoalFeasibilityService`) returns the derived %BW/wk pace + a verdict + a cap-paced realistic-date suggestion, with the verdict band shared (`verdictForRate`) between the preview and `GoalEvaluationService.gradeRate`. The band (≤`targetPctPerWeek` 0.7 → feasible · ≤`capPctPerWeek` 1.0 → feasible-with-warnings · over → aggressive) lives in exactly **one** method — `GoalFeasibilityService.verdictForRate` (single source of truth) — so the wizard's live preview and the persisted eval gate can never diverge. FE: the 2-step `GoalPlannerPage` wizard + the live feasibility preview in the cél step consume it (`useGoalCreation` no longer PUTs the profile; `useFeasibilityPreview`). |
| **Train** (meso · workout-exec · sport · catalog · records · running) | `api/feature/train/train.yml` | ✅ `feature/train` | `data/train/trainApi.ts` + `data/train/runningApi.ts` → `trainHooks.ts` / `runningHooks.ts` | ✅ Real, dual-mode. |
| **Fuel — Pantry (Kamra)** | `api/feature/pantry/pantry.yml` | ✅ `feature/pantry` | `data/fuel/pantryApi.ts` → `usePantry()` + `usePantryActions()` | ✅ Real, dual-mode (slice C, `mezo-9xu`). Single `pantry_item` table (Model B), `kind` discriminator, kind-split projection — see §4c. |
| **Fuel — Stack / Protocol** | `api/feature/fuel/fuel.yml` | ✅ `feature/fuel` | `data/fuel/fuelApi.ts` → `useStack()`/`useProtocol()`/`useStackDay()` + `useStackActions()`/`useProtocolActions()` | ✅ Real, dual-mode (slice C · P2, `mezo-09g` → living-occurrence model + backend placement engine `mezo-vx9v`, ADR [0017](../decisions/0017-living-occurrence-protocol.md)). The **first Fuel-owned package** + `fuel.yml` fragment: `protocol` (a single LIVING per-user row, version-bumped in place — no more versioned selection snapshot) + `protocol_item` (→ `pantry_item`, one row per occurrence: `slotKey`/`dose`/`pinned`/`placementSource`/`placementReason`/`restDayFallback`) + `supplement_intake` (append-only taken-ledger); occurrence CRUD `POST/PATCH/DELETE /api/fuel/protocol/items[/{id}]` + `GET /api/fuel/protocol` + `/api/fuel/intake` + `DELETE …/intake/entry/{id}` — **the old whole-selection `POST /api/fuel/protocol` activate endpoint is RETIRED**. Zone placement is server-decided by `PlacementEngine` (rule table → pantry timing → `StackPlacementLlm` consumer-owned-port fallback, gated `mezo.feature.stack-placement-llm.enabled`), projected into a timed zone timeline FE-side by the pure `projectStackDay`. See `fuel.md` §4. |
| **Fuel — Settings** (planner cadence + caffeine cutoff) | `api/feature/fuel-settings/fuel-settings.yml` | ✅ `feature/fuel` (`FuelSettingsService` + **ungated `CaffeineCutoffResolver`**) | `data/fuel/fuelSettingsApi.ts` → `useFuelSettings()` / `useFuelSettingsActions()` | ✅ Real, dual-mode (`mezo-53su`). Per-user singleton `fuel_settings` (`meals_per_day` 3..6 + `caffeine_cutoff` HH:mm; migration `202607231933`); `GET/PUT /api/fuel/settings` (tag `FuelSettings` → `FuelSettingsApi`), gated `mezo.feature.fuel-settings.enabled`; **GET never 404s** — config-ghost 4/"14:00" from `FuelSettingsProperties`. The **ungated `CaffeineCutoffPort`/`CaffeineCutoffResolver`** is the single caffeine-cutoff source the habit `no_stim_after` metric now reads (`HabitProperties.caffeineCutoff` + `mezo.habit.caffeine-cutoff` removed — works even with the fuel-settings surface off, the `SleepAnchorPort` idiom). Relocates `mealsPerDay` off the weight goal. See `fuel.md` §4. |
| **Fuel** (rest — day/timeline/week, recipes) | — | ❌ | `data/fuel/fuel.ts`, `data/fuel/pantry.ts`, `data/fuel/fuelWeek.ts` | 🔶 mock-only. |
| **Progression** (skill XP · level-up ledger · perk unlocks) | `levelUp` field on the **Train** `WorkoutInstanceResponse` (gym finish) + `RunSessionLogResponse` (run log) + own fragment `api/feature/progression/progression.yml` (`GET /api/progression/profile`, P4) | 🟡 `feature/progression` — P1 foundation + **P2 gym engine** + **P3a run engine** + **P3b sport engine** + **P4 profile API** | — (FE still mock; cards wire in P6) | 🟡 P1+P2 (gym) + P3a (run) + **P3b (sport)** + **P4 (profile)** done (`mezo-8e4`): curve math, 3 owned tables, entities/repos, perk catalog, the shared `award(...)` tail, `Gym`/`Run`/`SportSignalCalculator` + `ProgressionService.applyGym`/`applyRun`/`applySport`, the switch-gated `finishWorkout`/`logSession`/`logSportSession` triggers returning `levelUp`, and the switch-gated `ProgressionController` read surface (`getProfile`). FE cards + dual-mode mock (P6) remain. **Growth E3 (`mezo-6ng8`)** grew the fragment: `getProfile` += `savingsHuf30d` (trailing-30-day financial-activity sum, nullable) and a new `GET /api/progression/growth-week/{date}` → `GrowthWeekResponse` (ISO-week aggregate — quests/LIFE XP/activities/savings; honest zeros) fed by `GrowthWeekService` over the new `ActivityLedgerSource` port. **Me Growth page (`mezo-rmhr`)** adds `GET /api/progression/achievements` → `AchievementsResponse` (9 badges **derived on read** + perk unlocks, `AchievementService`, no DDL). All on the switch-gated `ProgressionController`. **`mezo-huzd`** adds a business-date column (`occurred_on`) to `level_up_event`, set explicitly by every `award(...)` call, and a new outgoing hook — `AccountProgressPort`, fired once per newly-created event — consumed by the sibling **gamification** ledger (own row below, §4f). See [`growth.md`](growth.md). See §4e/§4f. |
| **Gamification** (account-wide XP level/coins/streak/titles ledger) | `api/feature/gamification/gamification.yml` (tag `Gamification` → `GamificationApi`) | ✅ `feature/gamification` (switch `mezo.feature.gamification.enabled`) | `data/gamification/gamificationApi.ts` → `useGamification()`/`useTitles()`/`useGamificationActions()`/`useGamificationDay()` | 🟢 **`mezo-huzd`** — real backend (was FE-first mock-only since `mezo-k7rn`). `GET /api/gamification/profile` (totalXp Σ-derived from `skill_progress` + the account level curve), `GET /api/gamification/day/{date}` (the Harvest read — `level_up_event`/`coin_event` by business date), `POST /api/gamification/title/{key}/{buy,equip}` + `POST /api/gamification/saver/buy`. Coins/streak are driven server-side by `AccountProgressPort` (fired from the SAME `ProgressionService.award(...)` tail every XP source rides — GYM/RUN/SPORT/QUEST/ACTIVITY/HABIT). FE `fetchDerivedGamification` interim retired; `useGamificationActions` now mutates for real (`canMutate: true` both modes). Mock mode unchanged (own local ledger). The profile mapping is honest end-to-end: `streakAlive` rides the wire response through `gamificationApi.toProfile` into `GamificationProfile`, and `AppHero` dims the 🔥 chip when it's `false`. **The day-Harvest read's first UI consumer is the ritual's Harvest act (`mezo-ilsj`)** — see [`ritual.md`](ritual.md) §5. See §4f / [`growth.md`](growth.md). |
| **Ritual** (sleep-anchored Napzárás closing flow) | `api/feature/ritual/ritual.yml` (tag `Ritual` → `RitualApi`) | ✅ `feature/ritual` (switch `mezo.feature.ritual.enabled`, `mezo-hvmx`) | `data/ritual/ritualApi.ts` → `useRitualDay()`/`useRitualActions()` | 🟢 `GET /api/ritual/day/{date}` (never 404 — window derived fresh from `SleepAnchorPort`: `opensAt`=bed−75′, `prepStartsAt`=bed−45′) + `POST /api/ritual/close` (today-only, idempotent, 409 `RITUAL_NOT_TODAY`) + — **Phase 5 W1.2, `mezo-b3pp.2`** — `PUT /api/ritual/reflection` (`{date, text}`, today-only, upserts the day's prose onto the same `(created_by, ritual_date)` row **before** the close; text is `strip()`ped, blank is a CLEAR that creates no row). That third endpoint is what made `closed` mean `closed_at is not null` rather than "a `ritual_day` row exists" — a reader-visible contract semantics change, see [`ritual.md`](ritual.md) §4. Awards nothing itself — completion XP rides the existing HABIT tail (`evening_ritual`). Closing (or editing a closed day's prose) publishes `RitualClosedEvent` → companion's `ReflectionEmbeddingListener` → `memory_embedding(kind=reflection)`. The FE 6-act flow (`mezo-ilsj` + `mezo-b3pp.2`) is the primary consumer of both this contract and the Gamification day-Harvest read above. See [`ritual.md`](ritual.md). |
| **Needs** (Életjel-ringek day-close, `mezo-dhzk`) | `api/feature/needs/needs.yml` (tag `Needs` → `NeedsApi`) | ✅ `feature/needs` (switch `mezo.feature.needs.enabled`) | `data/needs/needsApi.ts` + `needsHooks.ts` → `useNeedsSummary()` (read); the write rides `useRitualActions().close(rings)`, no needs-owned mutation hook | 🟢 The six rings themselves are a CLIENT-SIDE live decay/refill sim (`features/today/logic/useNeeds.ts`) over existing app data — this contract only persists the day-**close** snapshot. `POST /api/needs/day-close` (idempotent per date — repeat calls return the stored result, no double award; 409 `NEEDS_NOT_TODAY`) awards bonus XP (`NeedsProperties`: `green-threshold`/`per-ring-xp`/`all-green-bonus-xp`) and advances the streak; `GET /api/needs/summary` (streak + last close, honest zeros before any close) never 404s. The ritual's Harvest act (`RitualPage`) fires the close as a best-effort tail of `POST /api/ritual/close` — a failed needs award never blocks the napzárás itself. See [`needs.md`](needs.md). |
| **Insights** | — | ❌ | `data/insights/insights.ts` | 🔶 mock-only. |
| **People** | `api/feature/people/people.yml` (`GET /api/people` bootstrap, `POST /api/people/{id}/mentions`) | ✅ `feature/people` — `person`+`mention` tables, mention stats server-computed | `data/me/peopleApi.ts` + `peopleHooks.ts` → `usePeople()` dual-mode | ✅ **Slice E (`mezo-t16y.2`)** — persons seed-only v1 (demodata; demo feed `demofixtures`); see `me.md` §4/§9. |
| **AI brain / Companion** (Spring AI, pgvector, RAG) | `api/feature/companion/companion.yml` (V0.2–V0.5) | 🟡 `feature/companion` — V0.1 port + **V0.2 chat spine** + **V0.3 context snapshot** + **V0.4 SSE stream** + **V0.5 tools/audit** | `data/insights/chatApi.ts` + `chatHooks.ts` → `useChat()`/`useChatActions()` (V0.4; real chips V0.5) | 🟡 Phase-3 in progress. **V0.1** (`mezo-fnnq.1`, [ADR 0008](../../docs/decisions/0008-companion-llm-spring-ai-2-gemini.md)): Spring AI 2.0 BOM + Gemini starter in `pom.xml`; `CompanionLlm` port + real/fake adapters behind `mezo.feature.companion.enabled` (fake under the `companion-fake` profile — ITs never touch the network); tiers in `mezo.companion.llm.*`. **V0.2** (`mezo-fnnq.2`): the chat spine — `ai_conversation` + `ai_message` tables (typed-but-null `tool_calls`/`refs` jsonb), the `companion.yml` fragment (`GET/POST /api/companion/conversation`, `GET .../messages`, `POST .../message` — sync JSON), `ConversationService` + `ChatService` (static HU voice + last-20-msg windowing → `CompanionLlm.complete`); every bean switch-gated (off ⇒ `/api/companion/*` 404s). **V0.3** (`mezo-fnnq.3`): `ContextSnapshotAssembler` — server-internal (no contract change), composes six other features' reads into every system prompt; config `mezo.companion.snapshot.*`. **V0.4** (`mezo-fnnq.4`): `POST .../message/stream` — SSE `delta`/`done`/`error`, two-transaction turn (`prepareTurn`/`completeTurn` + `ChatStreamService`), the **hand-written-controller contract precedent** (§9), and the FE goes real (ChatPage dual-mode, `apiSse` fetch-stream client, degraded switch-off state; deployed k3s keeps the switch off until a real `GEMINI_API_KEY` lands). **V0.5** (`mezo-fnnq.5`): tool calling — server-internal (contract change = description text only: `MessageResponse.tools`/`refs` fill up, `MessageTool.name` carries args baked in, e.g. `get_recovery(scope=sleep, days=3)`); 8 read tools at V0.5, **expanded to 15 scope-consolidated hub-tools at mezo-xixu** (per-turn budget raised 6→15) + `RecordingToolCallback` audit into the V0.2 jsonb envelopes; IDENT-2 ArchUnit rule. **V1.1** (`mezo-fnnq.6`): `knowledge_fact`/`learned_fact` tables + fact CRUD (`GET/POST /api/companion/fact`, `PATCH .../fact/{id}` — the app's **first PATCH**, partial-update semantics, `patchForBody` added to `ApiIntegrationTest`) + top-N prompt injection. **V1.2** (`mezo-fnnq.7`): candidate inbox + decision (`GET .../fact/candidate`, `POST .../candidate/{id}/decision`), post-turn async extraction (`ChatTurnCompleted` AFTER_COMMIT listener, `@EnableAsync` born in `techcore/configuration/AsyncConfiguration`), FE `useKnowledge`/`useKnowledgeActions` real. **V1.3** (`mezo-fnnq.8`): advisor chain (`feature/companion/advisor/` — clinical regex + LLM verdict, retry-once → degraded) — server-internal; contract change = `MessageResponse.degraded` required boolean. pgvector at V2.1. See [`companion.md`](companion.md) §3–§5. |
| **Proactive** (briefing + weekly suggestion + memoir + heartbeat + predictions + experiments) — ✅ **EPIC COMPLETE** | `api/feature/proactive/proactive.yml` | ✅ `feature/proactive` (dual-switch: companion + proactive; + six job switches `briefing`/`weekly-suggestion`/`memoir`/`heartbeat`/`prediction`/`experiment`) | ✅ Today briefing + `CompanionNoteCard` (H1) + Insights Weekly/Memoir/Predictions/**Experiments** tabs, all with honest fallback — **all Insights tabs real** | 🟢 B1.2+W1+W2+H1+P1+P2 — the read GETs (briefing/weekly-suggestion/memoir/heartbeat) plus **P1 `GET /api/proactive/prediction`** (LIST, smart-tier grounded in CONFIRMED patterns, deterministic validation; `200 []`=honest) and **P2 `GET /api/proactive/experiment` + a WRITE path `POST …/experiment/{id}/decision` (L2 accept/dismiss, 404/**409**) + `POST …/experiment/propose`** (smart-tier proposal, deterministic outcome via the shared `MetricWindowEvaluator`, two crons `runPropose`+`runOutcome`). Reuses companion reads + `CompanionLlm` behind both `mezo.feature.companion.enabled` **and** `mezo.feature.proactive.enabled`. Error codes `PROACTIVE_EXPERIMENT_NOT_FOUND`/`_NOT_PROPOSED`. See [`proactive.md`](proactive.md). |
| **Quest** (gamified growth E1, `mezo-df7q`) | `api/feature/quest/quest.yml` | ✅ `feature/quest` (switch `mezo.feature.quest.enabled` + cron switch `quest-job`) | ✅ Today „Napi küldetések" card | 🟢 `GET /api/quest/day/{date}` (lazy generate + derived evaluate; `quests: []`=honest; levelUps ride the read) + `POST /api/quest/{id}/reroll` (404/**409** `QUEST_NOT_OFFERED`/`_NOT_TODAY`/`_REROLL_EXHAUSTED`/`_REROLL_NO_ALTERNATIVE`). XP via progression `award` (`source_type=QUEST`); `LevelUpResult` enums extended (`QUEST`/`LIFE`) in `train.yml`. **`mezo-rmhr`** adds `GET /api/quest/history?from&to` (rerolled excluded, `QUEST_INVALID_DATE_RANGE` on inverted bounds) for the Me Growth journal. See [`growth.md`](growth.md). |
| **Activity** (gamified growth E2, `mezo-jzca`) | `api/feature/activity/activity.yml` | ✅ `feature/activity` (switch `mezo.feature.activity.enabled`; classifier dual-gated + companion) | ✅ Today „Tevékenységnapló" card/sheet + Me GrowthCard | 🟢 `POST /api/activity` (free-text → companion cheap-tier classify → clamped/capped XP, `source_type=ACTIVITY`; completes a matching activity-mode quest) + `GET /api/activity/day/{date}` (`[]`=honest) + `POST /api/activity/{id}/category` (grant/override — XP move). Errors `ACTIVITY_TEXT_REQUIRED`/`_SKILL_UNKNOWN`/`_NOT_FOUND`. Profile fragment grew `life[]` + computed `traits` (`TraitCalculator`). **`mezo-rmhr`** adds `GET /api/activity/history?from&to` (`ACTIVITY_INVALID_DATE_RANGE`) for the Me Growth journal. See [`growth.md`](growth.md). |
| **Push notifications** (`Értesítés`) — ✅ **N1+N2+N3 all shipped** | `api/feature/notification/notification.yml` | ✅ `techcore/webpush` (in-house VAPID ES256 + RFC 8291 `aes128gcm`, zero new dependencies — [ADR 0014](../decisions/0014-own-webpush-implementation.md)) + `feature/notification` (switch `mezo.feature.notification.enabled` + cron switch `notification-dispatch-job`, both **on**) | `data/notification/{notificationApi,notificationPrefHooks,notificationScheduleWriter}.ts` → `usePushSubscription()`/`useNotificationPrefs()`/`useScheduleSnapshotWriter()` | 🟢 `POST/DELETE /api/notification/subscription` + dev-only `POST /api/notification/test` (N1) + `GET/PUT /api/notification/pref` (N2, code-default fallback, `400 NOTIFICATION_UNKNOWN_CATEGORY`) + `PUT /api/notification/schedule` (N3, `feWritten`-gated full replace). **`NotificationDispatchJob` is live** — the app's first per-minute `@Scheduled` cron, resolving all 11 categories via `AnchorResolver` (backend-native + proactive-prose + FE-schedule anchors) into the pure `DueEvaluator`, then handing the send to a separate `PushDispatchExecutor` `@Async` bean (scheduler pool is size 1 — inlining the send would starve every other cron). A real push reached Daniel's iPhone from the k3s backend on 2026-07-29. See §4g / [`_platform-notifications.md`](_platform-notifications.md). |
| **LLM usage** (read rollup + browsable audit log) | `api/feature/llm-usage/llm-usage.yml` (tag `LlmUsage` → `LlmUsageApi`) | ✅ `feature/llmlog` (read side of `mezo-2zyu` — `LlmUsageController`/`LlmUsageService` + `LlmLogRepository.{aggregateSince,aggregateByStatusSince,aggregateByFeatureSince,aggregateByModelSince,aggregateByUserSince,findCalls}`; payload retention — `mezo-1y3p`'s nightly `LlmLogRetentionJob` NULLs a row's payload columns after `mezo.llm-log.retention.payload-days` (90), cost/token metadata kept forever; logs an unconditional per-run heartbeat — row count + cutoff — since its first real firing is ~90 days out) | `data/me/llmUsageHooks.ts` → `useLlmUsageSummary()`/`useLlmUsageBreakdown()`/`useLlmCalls()`/`useLlmCall()` | 🟢 `mezo-h3gb` — `GET /api/llm-usage/summary` (calendar day/week/month `{callCount, costUsd, currency}` over `llm_log_history`, `Europe/Budapest` via `mezo.llm-log.report-zone`; **OWNER-only since `mezo-qw37.3`** — `costUsd` = `sum(cost_usd)` of priced rows, **null when none priced** — an estimate). FE: the "AI-használat" card on the Me Profil page, which since **`mezo-uakh`** links into the full **`/me/ai-usage`** AI-napló page (+ `/me/ai-usage/:id` detail) — see below, `me.md` §2 and [`beta-admin.md`](beta-admin.md). |
| **Admin** (beta console) | `api/feature/admin/admin.yml` (tag `Admin` → `AdminApi`) | ✅ `feature/auth` — `AdminController`/`AdminService` (invites, users, temp-password reset, status), OWNER-only via `CurrentUser.requireOwner()` | `data/admin/adminHooks.ts` → `useAdminInvites()`/`useAdminUsers()`/`useAdminActions()` | 🟢 `mezo-qw37.3` — see [`beta-admin.md`](beta-admin.md) §4 |
| **Journal** (free-prose entries + embed pipeline, Phase 5 W1.1, `mezo-b3pp.1`; decision journal + review loop, Phase 5 W1.4, `mezo-b3pp.4`) | `api/feature/journal/journal.yml` (tag `Journal` → `JournalApi`) | ✅ `feature/journal` (switch `mezo.feature.journal.enabled`) | `data/journal/journalApi.ts` → `journalHooks.ts` → `useJournalNotes()`/`useJournalActions()`; `data/journal/decisionApi.ts` → `decisionHooks.ts` → `useDecisions()`/`useDecisionActions()` | 🟢 `journal_entry` table (`occurred_on`/`text`/`source` ∈ `quickinput`\|`ritual`) behind `GET /api/journal?from&to` (ranged, newest-first) + `POST`/`PUT /api/journal/{id}`/`DELETE /api/journal/{id}` (soft-delete, 404 `JOURNAL_ENTRY_NOT_FOUND` off-owner). Create/update publish `JournalEntrySavedEvent`, delete publishes `JournalEntryDeletedEvent` — `JournalEmbeddingListener` (both companion **and** journal switches, `@ConditionalOnProperty` array-AND'ed) consumes them AFTER_COMMIT and re-embeds into `memory_embedding(kind=journal_entry)` via `MemoryEmbeddingWriter.writeJournal`/`.deleteJournalEmbedding` (update-in-place re-embed on edit). **W1.4** adds the sibling `decision_entry` table behind `GET`/`POST /api/journal/decision` + `PUT /api/journal/decision/{id}/review` (404 `DECISION_ENTRY_NOT_FOUND` off-owner; no delete/update endpoint) — context snapshot frozen server-side at create, `DecisionEntrySavedEvent` on both create and review drives `DecisionEmbeddingListener` → `MemoryEmbeddingWriter.writeDecision` (`memory_embedding(kind=decision)`, review re-embeds the same row with the outcome folded in). All three decision operations share the `Journal` tag, so — per `skipDefaultInterface=true` (§9 gotchas below) — they are implemented in the SAME `JournalController`, not a separate `DecisionController`. FE: `JournalSheet` (quick-add, dual Napló/Döntés mode) + the `/me/naplo` `JournalPage` (monthly grouping + edit + open-decisions block) + `DecisionReviewSheet` + the QuickInput Napló two-option picker (Aktivitás/Napló). See [`journal.md`](journal.md) / `companion.md` §5. |

Confirmed by grep: only `data/hooks.ts`, `data/train/trainHooks.ts`, `data/train/runningHooks.ts`, `data/me/weightHooks.ts`, `data/me/biometricHooks.ts`, `data/me/goalHooks.ts`, `data/me/peopleHooks.ts`, `data/fuel/pantryHooks.ts` reference `isMockMode`; `fuel.ts`/`insights.ts`/`people.ts`/`pantry.ts` (the static seeds) contain **no** `apiFetch` (real Pantry traffic flows through `data/fuel/pantryApi.ts`, not the `data/fuel/pantry.ts` seed). `api/base.yml` `info.title` is "mezo API".

**Driving specs/ADRs:** Phase-2 design `docs/superpowers/specs/2026-06-10-phase2-backend-design.md`; deploy ADR `docs/decisions/0001-deploy-on-k3s-argocd-learning-track.md`; roadmap `docs/milestones/roadmap.md`. House standards: `docs/references/*.md` (all linked below).

---

## 2. User-facing behavior

This is infrastructure — it has no direct UI. Its observable behavior is **the wire**:

- **Login/register UI (multi-user, `mezo-qw37` S1).** An `OWNER` account is seeded; every other account self-registers with an invite code via `RegisterPage` → `POST /api/auth/register` → `TokenResponse`. `AuthGate` (`app/auth/AuthGate.tsx`) persists the token (`tokenStore`, `localStorage`) and gates the whole app on a resolved `GET /api/auth/me`. Every subsequent request carries `Authorization: Bearer <token>`. See [`_platform-auth-security.md`](_platform-auth-security.md).
- **Real-mode data** is fetched live (empty backend → `null` → views ghost-guard). **Mock-mode** (`VITE_USE_MOCK=true`) serves synchronous static data with no backend — the seam each feature toggles is `isMockMode()` inside its hook.
- **Error surface.** A failed request throws `ApiError` (`messages: SystemMessage[]`, `status`); the user-facing text is resolved server-side from message codes — there is no hardcoded English/Hungarian text on the wire, only stable codes (`AUTH_LOGIN_INVALID_CREDENTIALS`, `VALIDATION_REQUIRED_FIELD`, …).

For the actual screen flows that *consume* this spine, see the per-feature docs (Today/Mai, Me/Én, Train/Edzés).

---

## 3. Architecture & data flow

```
                          api/feature/<x>/<x>.yml   (per-feature OpenAPI fragment — write FIRST)
                                   │  openapi-merge-cli  (api/generate/merge.yml)
                                   ▼
                          api/openapi.yml            (committed merged contract — source of truth)
                ┌──────────────────┴───────────────────┐
   openapi-generator (spring)                 openapi-typescript
   @ mvn generate-sources                     @ pnpm generate:api
                │                                       │
   io.mrkuhne.mezo.api.controller.<Tag>Api      frontend/src/data/_client/api.gen.ts (committed)
   io.mrkuhne.mezo.api.dto.*  (target/, gitignored)    │
                ▼                                       ▼
   Controller implements <Tag>Api          lib/<x>Api.ts  (request bodies `satisfies <Request>`)
        → Service (@Transactional writes)         │  apiFetch (data/_client/api.ts) + Bearer token
        → Mapper (MapStruct entity↔dto)           ▼
        → Repository (OwnedRepository)      data/hooks.ts  ── isMockMode() ──┐
        → Entity (OwnedEntity, @SQLDelete)         │  real                   │ mock
        → Liquibase DDL (ddl-auto: validate)       ▼                         ▼
                ▼                            apiFetch → backend         static initialData
            PostgreSQL                                                  (TanStack Query)
```

**The view→DB path, traced (weight slice, the smallest full CRUD):**

- View → `useWeight()` (`frontend/src/data/me/weightHooks.ts:11`) → `weightApi` (`frontend/src/data/me/biometricsApi.ts`) → `apiFetch` (`frontend/src/data/_client/api.ts`) → `POST /api/biometrics/weight` / `GET /api/biometrics/weight`.
- Backend: `WeightLogController implements WeightApi` (`feature/biometrics/weight/controller/WeightLogController.java`) → `WeightLogService` → `WeightLogMapper` → `WeightLogRepository extends OwnedRepository<WeightLogEntity>` → `WeightLogEntity` → table `weight_log`.

**Dual-mode behavior** (the FE seam): `isMockMode()` (`frontend/src/data/_client/mode.ts`, reads `VITE_USE_MOCK`, default mock) switches each hook between mock static data (TanStack Query `initialData`, synchronous, no network) and the real `*Api` client. Real-mode mutations `invalidateQueries`; mock-mode mutations emulate the server via `queryClient.setQueryData`. **Hook signatures are the FE's own stable contract and are NOT generated** — only the DTOs/types crossing the wire are.

---

## 4. Data model & API

### 4a. The contract layer (`api/`)

- `api/base.yml` — `info` / `servers` (`http://localhost:8090`) / global `bearerAuth` security; `paths: {}`. Merge base — its info/servers/security win.
- `api/common/common-schemas.yml` — `SystemMessage` (fields `level` enum ERROR/WARNING/INFO, `code`, `params`, `message`, `fieldName`, `type` enum REQUEST/FIELD, `exceptionTraceId` uuid) + `SystemMessageList` (array). **Every non-2xx response references `#/components/schemas/SystemMessageList`.**
- `api/generate/merge.yml` — the ordered `inputs:` list: **32 `inputFile` entries** today = `base.yml` + `common/common-schemas.yml` + **30 per-feature fragments** under `api/feature/`. Order: base → common → auth → weight → sleep → checkin → train → goal → biometrics-profile → … → needs → journal → **companion-feedback** (the newest, `mezo-b3pp.15`). (Two numbers, don't conflate them: 32 is what `grep -c 'inputFile:' api/generate/merge.yml` returns; 30 is the per-feature count that matters when you're asking how many features are contract-backed.) **Forgetting to append a new fragment here silently drops it from the merge** — no error, the endpoints just never appear in `api/openapi.yml` or `api.gen.ts`. Order matters only for the base (first wins on `info`/`servers`/`security`); everything after it is additive, so a new fragment goes on the end.
- `api/generate/package.json` — `openapi-merge-cli ^1.3.2`, script `generate:api`.
- `api/openapi.yml` — committed merged output. `frontend/src/data/_client/api.gen.ts` is its committed TS projection (`components['schemas']['X']`, `operations`, `paths`).

**Fragment naming rules (load-bearing — generated names derive from them):**

- schema name = generated class name (`LogWeightRequest`, `WeightLogResponse`); requests end `Request`, responses `Response`;
- `operationId` = controller method name (`logWeight`);
- `tag` = generated interface name (tag `Weight` → `WeightApi`; tag `Train` → `TrainApi`).

**A fragment is not required to match a backend feature package 1:1 — a TAG is what a controller binds to.** A new surface inside an existing feature can either grow that feature's fragment (sharing its tag, and therefore its controller — see the `skipDefaultInterface` trap in §9) or get its **own fragment + own tag**, which buys it its own generated `<Tag>Api` and its own controller. Live worked example, `mezo-b3pp.15`: the 👍/👎 feedback surface lives in the `feature/companion` **Java** package but ships as `api/feature/companion-feedback/companion-feedback.yml` under tag **`CompanionFeedback`** → `CompanionFeedbackApi` → `CompanionFeedbackController`, deliberately NOT folded into `companion.yml`'s `Companion` tag — that interface already carries conversations, facts, patterns and the memory observatory, and a third responsibility would have grown `CompanionController` again. Choose the second option whenever the new endpoints have their own lifecycle; it costs one line in `merge.yml`.

**Validation lives in the spec** (`required`, `minLength`, `minimum/maximum`+`exclusiveMinimum`, `pattern`, `format`) → JSR-303 on the backend. Never hand-add validation the spec can express. Two codified gotchas: `@NotBlank` has no OpenAPI equivalent → `required` + `minLength: 1` (an empty string then fails as `Size`→`VALIDATION_INVALID_VALUE`, **not** `VALIDATION_REQUIRED_FIELD`, which only fires for null/missing); and **prefer `pattern` over `enum` on *request* fields** — an invalid enum fails Jackson deserialization (500), a `pattern` fails bean validation (400). Live: `SportScheduleSlotInput.kind` uses `pattern: '^(training|match)$'` on the request while the response variant uses `enum`. Same idiom for the sport discriminator: `SportScheduleSlotInput.sport` uses `pattern: '^(volleyball|cross|trx)$'` (`mezo-05v6`) on the request, while `SportScheduleSlotResponse.sport` is a required plain string.

### 4b. The persistence layer (every owned table)

UUID PKs (`id UUID DEFAULT gen_random_uuid()` in DDL; `@Id @GeneratedValue @Column(columnDefinition="uuid")` in Java). Every owned table carries `created_by uuid NOT NULL` (FK→`app_user(id) ON DELETE CASCADE`), `is_deleted boolean`, `created_at` — supplied by the `OwnedEntity` `@MappedSuperclass`. Soft delete via `@SQLDelete(... set is_deleted = true ...)` + `@SQLRestriction("is_deleted = false")`. Typed jsonb via `@JdbcTypeCode(SqlTypes.JSON)` onto a *typed record*, never `String` (live: `MesocycleEntity.volumeRecompute`→`VolumeRecomputeJson`, `RunningBlockEntity.structure`→`RunningBlockStructure`, and the signature provenance envelope `ProvenanceEnvelope` persisted as one jsonb column in `muscle_group_volume_log.source`). Typed Postgres `text[]` via `@JdbcTypeCode(SqlTypes.ARRAY)` (`MesocycleEntity.phaseCurve` and, G1, `GoalEntity.guards` — both `List<String>` so Hibernate dirty-checks element changes). **Not every owned aggregate uses `OwnedRepository`** — that superclass's `findAllOwned` requires an `e.date` field, so **date-less owned aggregates extend `JpaRepository` directly** with bespoke `findBy…CreatedByAndDeletedFalse` finders (G1's `GoalRepository`/`BiometricProfileRepository`; both entities still `extends OwnedEntity` for the ownership+soft-delete columns). **Additive fields on a jsonb-backed record need no migration** — e.g. `RunPrescribedSession.timeOfDay` (nullable `HH:mm`) was added straight onto `RunningBlockStructure` and rides the existing `running_block.structure` column; the only contract touch was `api/feature/train/train.yml` (`pattern: '^\d{2}:\d{2}$'`, `nullable`) regenerated into `api/openapi.yml`, with **no Liquibase changeset**. **Derived fields are computed server-side, never trusted from the request** (same principle as `created_by`): e.g. `currentWeek` is set to `clampWeek(startDate, weeks)` in both `TrainService` (mesocycles) and `RunningService` (blocks), and the running read path additionally heals a stale/out-of-range stored value (mezo-478).

### 4c. REST endpoints (backed features only)

| Method + path (prefix `/api`) | Tag/Api | Purpose |
|---|---|---|
| `POST /auth/login` | `AuthApi` | obtain JWT (public, `security: []`) |
| `POST /biometrics/weight`, `GET /biometrics/weight` | `WeightApi` | log / list weight |
| `POST /biometrics/sleep`, `GET /biometrics/sleep` | `SleepApi` | log / list sleep (POST widened with enriched in-bed/phase/source fields, `mezo-dbsr`) |
| `GET/PUT /sleep/goal` | `SleepGoalApi` | sleep goal + day-anchor (`mezo-dbsr`) — per-user singleton; **GET never 404s** (config-ghost); gated on `mezo.feature.sleep-goal.enabled`. The ungated `SleepAnchorPort`/`SleepAnchorResolver` is the shared wake/bed derivation Habit + Fuel consume |
| `POST /sleep/screenshot` (multipart) | `SleepShotApi` (`SleepShotController`) | Sleep Cycle screenshot → editable draft (`mezo-66ab`, slice B) — gated on `mezo.feature.sleep-shot.enabled`; ONE vision call + deterministic `SleepShotDraftValidator` (confidence never from the LLM); **ephemeral** (nothing persisted — confirm via `POST /biometrics/sleep`, `source:'screenshot'`). Companion vision LLM via the sleep-owned `SleepShotLlm` port ([ADR 0012](../decisions/0012-consumer-owned-llm-ports.md)) — companion off ⇒ clean **503** `SLEEP_SHOT_LLM_UNAVAILABLE`; bad model answer ⇒ **502** `SLEEP_SHOT_EXTRACT_FAILED` |
| `POST /biometrics/checkin` (+read) | `CheckInApi` | daily check-in upsert |
| `GET/PUT /biometrics/profile` | `BiometricProfileApi` | body-composition profile (one row/owner; G1) |
| `GET/POST /goals`, `GET/PUT/DELETE /goals/{id}`, `POST /goals/{id}/{activate,archive}` | `GoalApi` | goal CRUD + lifecycle (single-active enforced in service; G1) |
| `GET /goals/{id}/timeline`, `POST /goals/{id}/plans`, `DELETE /goals/{id}/plans/{linkId}` | `GoalApi` | goal-plan-link timeline / attach / detach (G3, `mezo-3sc`) — see §4d |
| `POST /goals/{id}/evaluate` | `GoalApi` | run the TDEE/prescription engine (G5, `mezo-g1u`) — persists `prescription`+`tdeeBootstrap`, no-profile → 200 feasibility note |
| `POST /goals/feasibility-preview` | `GoalApi` | stateless realism preview (G6, `mezo-06n`) — derived %BW/wk pace + verdict + cap-paced realistic date; `GoalFeasibilityService`, no persistence |
| Train surface (`/train/...`, running, sport, gym-schedule, catalog, records) | `TrainApi` (fans to 7 services) | see Train feature doc + `api/feature/train/train.yml` |
| `GET /pantry` | `PantryApi` | the owner's pantry, projected by `kind` into `{ ingredients, stash }` (food → ingredients; supplement/stim/med → stash) — Fuel slice C, `mezo-9xu` |
| `POST /pantry`, `PUT/DELETE /pantry/{id}` | `PantryApi` | add / update / soft-delete a pantry item; per-kind required-field validation in `PantryService.validatePerKind` (see the single-table note below) |
| `GET /pantry-import/lookup?q=`, `POST /pantry-import` | `PantryImportApi` | OpenFoodFacts lookup proxy + confirmed-draft import (Fuel P6, `mezo-bka`) — a SEPARATE tag so the whole feature is bean-gated by `mezo.feature.pantry-import.enabled` (off → 404); paths deliberately outside `/api/pantry/*` (a static segment there would 405 into the `{id}` pattern when off) |
| `POST /pantry-import/scrape` | `PantryScrapeApi` (own `PantryScrapeController`) | URL-scrape draft (Fuel P8, `mezo-8vum`) — a SEPARATE tag/controller gated by `mezo.feature.pantry-scrape.enabled` (off → 404); returns an **ephemeral** draft (nothing persisted; the user confirms via `POST /pantry-import`). Needs the companion cheap LLM through the **pantry-owned `ScrapeLlm` port** ([ADR 0012](../decisions/0012-consumer-owned-llm-ports.md)) — companion off ⇒ no adapter bean ⇒ clean **503** `PANTRY_SCRAPE_LLM_UNAVAILABLE`; page fetch via the SSRF-guarded `WebPageClient` (below) |
| `POST /meal/ai-draft` | `MealAiLogApi` (own `MealAiDraftController`) | AI meal-draft from free text and/or a photo (Fuel P8, `mezo-78rn`) — a SEPARATE tag/controller gated by `mezo.feature.meal-ai-log.enabled`; **off → 405, not 404** (the `multipart` path collides with `/api/meal/{id}`, so the surviving route yields 405 — a NEW techcore `GlobalExceptionHandler.handleMethodNotAllowed` returns a clean `METHOD_NOT_ALLOWED` instead of a generic 500). Needs the companion cheap+vision LLM via the **meal-owned `MealDraftLlm` port** ([ADR 0012](../decisions/0012-consumer-owned-llm-ports.md)) — companion off ⇒ clean **503** `MEAL_AI_LLM_UNAVAILABLE`; the ephemeral draft is confirmed via `POST /meal` (widened with an `estimate` item arm + `provenance`) — see [`fuel.md`](fuel.md) §4 |
| `GET /recipe/{id}/breakdown` | `RecipeApi` (`RecipeController`) | Lazy template-breakdown read (`mezo-bw3y`) — the deterministic 3-dim envelope is recomputed each call; LLM prose enrichment (Hungarian summary/details/improve/fitsFor) rides the **recipe-owned `RecipeBreakdownLlm` port** ([ADR 0012](../decisions/0012-consumer-owned-llm-ports.md)) behind `mezo.feature.recipe-ai-score.enabled` and is persisted in `recipe.breakdown` jsonb only on success; the deterministic core stays ON with the flag/companion off (**silent prose-less degrade, no 503**) — see [`fuel.md`](fuel.md) §4 |
| `GET/POST /companion/conversation`, `GET .../{id}/messages`, `POST .../{id}/message` | `CompanionApi` | companion chat spine (V0.2, `mezo-fnnq.2`) — sync JSON turn; see [`companion.md`](companion.md) §4 |
| `POST /companion/conversation/{id}/message/stream` | `CompanionStreamApi` (**hand-written impl** — the V0.4 SSE precedent, §9) | streamed chat turn (`text/event-stream`: `(delta\|tool)*, done\|error` — the live `tool` event added mezo-280), `mezo-fnnq.4` |
| `POST /companion/transcribe` (multipart) | `CompanionVoiceApi` (`CompanionVoiceController`) | chat voice input (`mezo-at8x.4`) — multipart `audio` → `{text}`; **its own tag** so the generated interface stays separate from the conversation CRUD surface. Ephemeral like `/sleep/screenshot`: ONE multimodal call, nothing persisted. Service-level size/mime caps (`mezo.companion.transcription.*`, base-mime match — `MediaRecorder` writes `;codecs=opus`) → 400 `VALIDATION_INVALID_VALUE`; a narrating model ⇒ **502** `COMPANION_TRANSCRIBE_FAILED`; **empty transcript is a 200** (silence is not an error). Companion off ⇒ the whole surface 404s |
| `GET/POST /companion/fact`, `PATCH /companion/fact/{id}` | `CompanionApi` | knowledge-fact CRUD (V1.1, `mezo-fnnq.6`) — first PATCH endpoint (partial update); see [`companion.md`](companion.md) §4 |
| `GET /companion/fact/candidate`, `POST .../candidate/{id}/decision` | `CompanionApi` | extraction-candidate inbox + accept/refine/reject decision (V1.2, `mezo-fnnq.7`) |
| `GET /companion/feedback?kind&ids`, `PUT /companion/feedback`, `DELETE /companion/feedback/{artifactKind}/{artifactId}` | `CompanionFeedbackApi` (own fragment `api/feature/companion-feedback/`, own tag → own `CompanionFeedbackController`) | 👍/👎 on AI-produced artifacts (Phase 5 W4.1, `mezo-b3pp.15`) — batch page-hydration read (comma-joined `ids`, `minItems 1`/`maxItems 200`; unvoted ids are absent, never an error), upsert, and an **idempotent 204** retract. **No switch of its own** — rides `mezo.feature.companion.enabled`, so all three 404 when the companion is off. The reason/verdict cross-field rule is a service-level 400 `FEEDBACK_REASON_REQUIRES_DOWN` in front of the DB CHECK (see §9). Table + semantics: [`companion.md`](companion.md) §4 |
| `GET /ritual/day/{date}`, `POST /ritual/close`, `PUT /ritual/reflection` | `RitualApi` | daily closing-ritual state/window + idempotent today-only close (`mezo-hvmx`) + the today-only evening-prose upsert (`mezo-b3pp.2` — blank text clears, and `RitualDayResponse` carries `reflectionText`) — window from `SleepAnchorPort`; 409 `RITUAL_NOT_TODAY` on both writes. All three share the `Ritual` tag, so all three land on `RitualController` (the one-controller-per-tag rule, §9). See [`ritual.md`](ritual.md) and the [ritual design spec](../superpowers/specs/2026-07-24-daily-closing-ritual-design.md) |
| `POST /needs/day-close`, `GET /needs/summary` | `NeedsApi` | Életjel-ring day-close snapshot (idempotent per date, awards bonus XP + advances the streak; 409 `NEEDS_NOT_TODAY`) + the streak/last-close read (honest zeros, never 404) — `mezo-dhzk`. Gated on `mezo.feature.needs.enabled`. See [`needs.md`](needs.md) |
| `GET /gamification/profile`, `GET /gamification/day/{date}` | `GamificationApi` | account XP/level/coins/streak/titles (`totalXp` server-summed + the account level curve) + the day Harvest aggregate (`mezo-huzd`) — honest zeros/ghost, never 404 while the switch is on |
| `POST /gamification/title/{key}/buy`, `POST /gamification/title/{key}/equip`, `POST /gamification/saver/buy` | `GamificationApi` | shop-title purchase (auto-equips) / equip an owned-or-unlocked title / streak-saver purchase — 404 `GAMIFICATION_TITLE_UNKNOWN`, 409 `_OWNED`/`_NOT_SHOP`/`_LOCKED`/`GAMIFICATION_COINS_INSUFFICIENT`/`GAMIFICATION_SAVER_LIMIT`. See §4f |
| `POST /notification/subscription`, `DELETE /notification/subscription?endpoint=` | `NotificationApi` | register (upsert on `(created_by, endpoint)`, idempotent) / unregister (idempotent — unknown endpoint is also 204) a device's Web Push subscription. N1, `mezo-h4wp.6.1`. See §4g |
| `POST /notification/test` | `NotificationApi` | dev-only fixed test push, fan-out to every live device → `PushTestResponse{attempted,sent}` — never errors even when nothing is reachable; switch-gated. See §4g |
| `GET/PUT /notification/pref` | `NotificationApi` | per-category enabled + lead-minutes; GET always reports all 11 (stored row or code default); PUT upserts one or more, `400 NOTIFICATION_UNKNOWN_CATEGORY` on a bad key. N2, `mezo-h4wp.6.2`. See §4g |
| `PUT /notification/schedule` | `NotificationApi` | replaces the FE-owned recurring schedule for the named categories (`checkin`/`fuel_slot` only — `400 NOTIFICATION_UNKNOWN_CATEGORY` also fires on a recognized-but-backend-native key). N3, `mezo-h4wp.6.3`. See §4g |
| `GET /llm-usage/summary` | `LlmUsageApi` | day/week/month call-count + summed cost over `llm_log_history` (`mezo-h3gb`) — calendar periods in `mezo.llm-log.report-zone`; **OWNER-only** (`mezo-qw37.3`, 403 `AUTH_FORBIDDEN`); still one number for ALL accounts — `LlmUsageSummaryResponse` carries no `byUser`/`userId` split, unlike `/breakdown`; `costUsd` null when no priced rows in the period |
| `GET /llm-usage/breakdown?period=` | `LlmUsageApi` | totals (call count, success/error/cancelled split, unpriced count, summed cost) + `features[]`/`models[]` rollups for ONE calendar period (`mezo-uakh`) — cost-descending, unpriced last; `period` is a **pattern-validated string** (`DAY\|WEEK\|MONTH`) rather than an OpenAPI enum — the choice predates `mezo-x0nb`, which has since added the `MethodArgumentTypeMismatchException` handler that makes an enum-typed query param 400 rather than 500; the string form stays because it works and is validated twice (contract `pattern` + `UsagePeriod.parse`); **OWNER-only** (`mezo-qw37.3`, 403 `AUTH_FORBIDDEN`); also gained `byUser: LlmUsageUserGroup[]` (`mezo-qw37.3`, per-account split, cost-descending, unpriced last, null user = background) — see [`beta-admin.md`](beta-admin.md) §4; `costUsd` null wherever no row in that bucket is priced |
| `GET /llm-usage/calls?period=&feature=&status=&callKind=&userId=&limit=` | `LlmUsageApi` | newest-first list of audited calls for a period, metadata only — **payload columns (`systemPrompt`/`userMessage`/`responseText`) are deliberately excluded from this projection** (`mezo-uakh`); `feature`/`status`/`callKind` are optional server-side filters (pattern-validated strings, same 400-not-500 reasoning as `period`); `limit` (1..500, default 50) is a **growing window, not a page offset** — the repository fetches `limit + 1` rows so `hasMore` is derived without a second `count(*)`; **OWNER-only** (`mezo-qw37.3`, 403 `AUTH_FORBIDDEN`); flat `LlmCallListResponse{items, hasMore}`, no `byUser` here — also gained `userId` (uuid, `mezo-qw37.3`, filters `l.createdBy` server-side to narrow the list to one account) and `createdBy` on every `LlmCallListItem` |
| `GET /llm-usage/calls/{id}` | `LlmUsageApi` | one audited call in full, incl. the verbatim payload columns + the frozen `pricingSnapshot` (`mezo-uakh`); 404 `LLM_LOG_CALL_NOT_FOUND` on an unknown id; **OWNER-only** (`mezo-qw37.3`, 403 `AUTH_FORBIDDEN`) — the log has rows with no owner at all (cron/stream), so ownership-by-`created_by` is not the gate; `payloadScrubbedAt` (`mezo-1y3p`) is non-null once the nightly `LlmLogRetentionJob` has NULLed the payload columns of a row older than `mezo.llm-log.retention.payload-days` (90) — cost/token metadata is kept forever |
| `POST /admin/invites` `{label?, expiresInDays?}` | `AdminApi` | mints a readable `MEZO-XXXX-XXXX` invite code (`mezo-qw37.3`); OWNER-only (403 `AUTH_FORBIDDEN`); 400 on bad `expiresInDays` (1..365) — see [`beta-admin.md`](beta-admin.md) §4 |
| `GET /admin/invites` | `AdminApi` | lists invites newest-first, incl. used ones (`usedBy`/`usedByName`/`usedAt`); OWNER-only |
| `DELETE /admin/invites/{id}` | `AdminApi` | deletes an open invite, 204; OWNER-only; 404 `ADMIN_INVITE_NOT_FOUND`, 409 `ADMIN_INVITE_USED` on an already-used code |
| `GET /admin/users` | `AdminApi` | lists accounts oldest-first (`role`/`status`/`onboardedAt`/`lastSeenAt`); OWNER-only |
| `POST /admin/users/{id}/reset-password` | `AdminApi` | mints a 12-char temporary password, sets `must_change_password = true`, returns it in clear text (the only copy); OWNER-only; 404 `ADMIN_USER_NOT_FOUND` |
| `POST /admin/users/{id}/status` `{status}` | `AdminApi` | flips `ACTIVE ↔ DISABLED`, 204; OWNER-only; 400 on a bad status, 404, 409 `ADMIN_SELF_STATUS` on the caller's own row or any OWNER row |
| `GET /journal?from&to`, `POST /journal`, `PUT /journal/{id}`, `DELETE /journal/{id}` | `JournalApi` | free-prose journal CRUD (Phase 5 W1.1, `mezo-b3pp.1`) — ranged listing newest-first, create defaults `occurredOn` to today, update/delete 404 `JOURNAL_ENTRY_NOT_FOUND` off-owner. Create/update/delete publish `JournalEntrySavedEvent`/`JournalEntryDeletedEvent`, consumed AFTER_COMMIT by `JournalEmbeddingListener` (gated on **both** `mezo.feature.companion.enabled` and `mezo.feature.journal.enabled`) to keep `memory_embedding(kind=journal_entry)` in sync. Gated on `mezo.feature.journal.enabled` alone for the CRUD surface itself — companion off still serves the journal, just without embeds. See [`journal.md`](journal.md) |
| `POST /journal/decision`, `GET /journal/decision`, `PUT /journal/decision/{id}/review` | `JournalApi` | decision journal + review loop (Phase 5 W1.4, `mezo-b3pp.4`) — create freezes a server-rendered context snapshot (`ContextSnapshotAssembler.render`, never client-supplied, never returned over the wire); list is newest-first, no range params; review is rate-only (`outcomeRating` 1-5 + optional `outcomeText`) — there is no update/delete endpoint for a decision. 404 `DECISION_ENTRY_NOT_FOUND` off-owner. Create/review both publish `DecisionEntrySavedEvent`, consumed AFTER_COMMIT by `DecisionEmbeddingListener` (same dual-switch gating as journal) to keep `memory_embedding(kind=decision)` in sync — review re-embeds the SAME vector row with the outcome folded into the content. **Implemented in `JournalController`, not a separate `DecisionController`** — see §9 "One controller per tag" below. See [`journal.md`](journal.md) |

**The Pantry single-table model (Model B, `mezo-9xu`).** All pantry rows live in one `pantry_item` table (migration `db/changelog/1.0.0/script/202606221200_mezo-9xu_create_pantry_item.sql`), with a `kind` column (`food`/`supplement`/`stim`/`med`) acting as the discriminator. `feature/pantry/service/PantryService.getPantry` reads the owner's items once and **projects them by kind** into the `PantryResponse` shape `{ ingredients, stash }` — food rows map to `IngredientResponse`, supplement/stim/med rows to `SupplementStashResponse` (`PantryMapper`). Rather than DB CHECK constraints per kind, **per-kind required fields are validated in the service** (`PantryService.validatePerKind` — e.g. `food` needs macros/serving fields) so the single table stays flexible; entity micros are typed jsonb (`MicroFact` list via `@JdbcTypeCode(SqlTypes.JSON)`), with the usual UUID PK + `created_by` ownership + soft-delete columns. Since Fuel P6 (`mezo-bka`) the backend also owns the **import + suggestion read surfaces**: `PantryResponse` carries required `imports[]` (the `pantry_import` feed table, capped `mezo.pantry-import.feed-size`) + `suggestions[]` (computed per read by `PantrySuggestionService` — deterministic cheaper-alt/low-NOVA swap heuristics, no table), and **`OffClient` is the app's first outbound HTTP client** (Spring `RestClient` via `spring-boot-starter-restclient`; OFF text/barcode lookup, `nova_group` passthrough, 502 `PANTRY_IMPORT_LOOKUP_FAILED` on upstream failure). Recipe scoring shipped in P7 (`feature/nutrition`). **Fuel P8 (`mezo-8vum`) shipped the URL-scrape import:** `WebPageClient` is the app's **second outbound HTTP client** (Spring `RestClient`, SSRF-guarded + no redirect-follow + streamed size cap) fetches the product page, `HtmlNutritionStripper` (jsoup) reduces it to prompt text, and `ScrapeExtractionService` turns it into a draft via one cheap-tier LLM call over the **pantry-owned `ScrapeLlm` port** (companion supplies the adapter, [ADR 0012](../decisions/0012-consumer-owned-llm-ports.md); companion off ⇒ clean 503). The draft is ephemeral — persisted only when the user confirms it through the P6 import path. See [`fuel.md`](fuel.md) §4–§5.

**Mock-only domains (no backend, no fragment):** the rest of Fuel (day/timeline/week, recipes, stack, protocol) lives in `frontend/src/data/fuel/fuel.ts` / `pantry.ts` (the static seed, distinct from the now-backed Pantry inventory) / `fuelWeek.ts`; Insights in `data/insights/insights.ts`. (Insights **Weekly** went dual-mode in D′ `mezo-t16y.1` **without** a backend/fragment — `useWeekly` composes it client-side over existing Fuel/Train/biometrics contracts; the one contract touch was Train's new `GET /api/train/workouts?from&to`. Chat/Patterns/Knowledge are backed by the companion — see `companion.md`.) (People left this list in Slice E `mezo-t16y.2` — `data/me/people.ts` is now the mock-mode seed only.) The backend will plug in by adding `api/feature/<x>/<x>.yml`, a `feature/<x>` backend package, and swapping the mock hook to dual-mode — exactly the recipe in §7. (`ProvenanceEnvelope`'s docstring already forward-references "Fuel reuses this pattern for meal score".)

### 4d. The goal-plan-link aggregate (G3 — timeline coupling, `mezo-3sc`)

The first **cross-feature** owned aggregate: it positions an owned *train* plan on a *goal*'s timeline. The table `goal_plan_link` (`db/changelog/1.0.0/script/202606181600_mezo-3sc_create_goal_plan_link.sql`) carries `goal_id` (FK→`goal ON DELETE CASCADE`), `plan_type` (CHECK `('mesocycle','running_block')`), `plan_id`, and a `[start_week, end_week]` 1-based span (CHECK `start_week >= 1 AND end_week >= start_week`), plus the usual `OwnedEntity` columns. Entity `feature/goal/entity/GoalPlanLinkEntity.java:29` — a **date-less owned aggregate**, so its repository `feature/goal/repository/GoalPlanLinkRepository.java:9` extends `JpaRepository` with bespoke `findByGoalIdAndCreatedByAndDeletedFalseOrderByStartWeekAsc` / `findByIdAndCreatedByAndDeletedFalse` finders (same pattern as `GoalRepository`).

Three endpoints fan off the existing `GoalController` (`feature/goal/controller/GoalController.java:64-76`) into two new services:

- **`GoalPlanLinkService`** (`feature/goal/service/GoalPlanLinkService.java`) — the write/resolve side. `attachPlan` (`:46`) ownership-checks the goal **and** the referenced plan, then derives `endWeek = startWeek + plan.weeks - 1` server-side (`:55`) — `end_week` is **never trusted from the request**, matching the `created_by`/`currentWeek` derived-field rule (§4b). `detachPlan` (`:60`) soft-deletes via `@SQLDelete` after a this-goal ownership filter. `resolvePlan` (`:69`) reads the train repos **READ-ONLY** to build the display `GoalPlanRef` (title/status/dates/weeks). **The only train-side change is one additive read finder** `MesocycleRepository.findByIdAndCreatedByAndDeletedFalse` (`feature/train/repository/MesocycleRepository.java:19`); the running counterpart already existed. Train behavior is unchanged.
- **`GoalTimelineService`** (`feature/goal/service/GoalTimelineService.java`) — the read assembly. `getTimeline` (`:43`) lists the links + resolves each plan-ref, then scans gym-lane coverage: only `mesocycle`-type links tile coverage (`:53`), running blocks are episodic, volleyball is ambient and never a link. Weeks no mesocycle spans become soft, non-blocking `GoalGap`s (`:62-73`); the window length is derived from the goal's `startDate..targetDate` span (`:84`), not stored. Pure read — no `@Transactional`.

**Goal-upsert inverted-window guard (G4b, `mezo-tji`/`mezo-b0k`).** `GoalService.applyUpsert` (`feature/goal/service/GoalService.java:94-101`) rejects an inverted window **up front**: when both dates are present and `targetDate.isBefore(startDate)`, it throws `SystemRuntimeErrorException(SystemMessage.field("VALIDATION_INVALID_VALUE", "targetDate"), HttpStatus.BAD_REQUEST)` → a **400 FIELD error on `targetDate`** for both POST `/goals` and PUT `/goals/{id}`. Before the guard, a `targetDate < startDate` upsert persisted, then the next `GoalTimelineService.getTimeline` derived a **negative** window length (`:84`) and blew up with a **500** — the guard moves the failure to the write boundary as a clean validation error. The `GoalPlannerPage` wizard already gates `targetDate > startDate` in the UI (`canNext`), so this is the server-side belt-and-braces; the date range itself stays expressible only in code (no OpenAPI cross-field constraint), so it lives in `applyUpsert`, not the spec. IT: `feature/goal/GoalServiceIT` covers the inverted-date 400.

`GoalPlanLinkMapper` (`feature/goal/mapper/GoalPlanLinkMapper.java:22`) maps `(entity, resolved GoalPlanRef) → GoalPlanLinkResponse`, converting the `String planType` to the generated inner enum via `fromValue`. **Cascade on goal delete** is handled in Java, not the DB FK: `GoalService.deleteGoal` (`feature/goal/service/GoalService.java:63-67`) soft-deletes the goal's links first (the FK `ON DELETE CASCADE` only fires on a *physical* delete, which the soft-delete path never triggers), so a re-used goal id never inherits ghost links. Demodata links the demo meso + running block to the demo goal (`feature/goal/GoalSeedData.java`, `demodata` profile). Contract fragment: the same `api/feature/goal/goal.yml` (POST `/plans` → **201**, DELETE `/plans/{linkId}` → **204**, GET `/timeline` → **200**; schemas `GoalPlanAttachRequest`/`GoalPlanLinkResponse`/`GoalPlanRef`/`GoalTimelineResponse`/`GoalGap`). ITs: `feature/goal/{GoalPlanLinkServiceIT,GoalTimelineServiceIT,GoalTimelineContractIT}.java`. FE read-side: `data/me/goalLinkApi.ts` → `useGoal()` builds real `linkedMesocycles` + `goal.mesocycles` from the timeline (see Me feature doc + `_platform-data-layer.md`).

### 4e. The progression domain (P1 foundation + P2 gym-path + P3a run-path + P3b sport-path engines + finish/log triggers + P4 profile read surface, `mezo-8e4`)

> **Gym + run engines + their triggers landed; standalone HTTP read surface still deferred.** P1 landed the persistence + pure-logic spine for the skill-progression / "level-up" system; **P2 (gym path)** added the engine that *grants* XP — `GymSignalCalculator` (extracts a `GymSignal` from a finished gym instance) + `ProgressionService.applyGym` — and wired it into the gym finish flow (`WorkoutService.finishWorkout`). **P3a (run path)** extends the same engine to the RUN family: it first **extracted the GYM-agnostic tail of `applyGym` into a shared private `award(...)`** (the deltas→upsert→gains→perks→robustness→event→payload part, now reused by every family), then added `RunSignalCalculator` + `ProgressionService.applyRun` (see below) and wired it into the run-log flow (`RunningService.logSession`). The gym finish returns a `levelUp` on `WorkoutInstanceResponse`; **the run log now also returns a `levelUp` on `RunSessionLogResponse`** — both ride the existing Train contract (no new fragment), both behind the same `mezo.feature.progression.enabled` switch. **P3b (sport path)** generalized the `sport_session` entity into a 3-kind modality (the existing `sport` column became the `volleyball|cross|trx` discriminator via `ck_sport_session_sport`, plus a `rounds` effort column — `202606271000_mezo-lmox_…`) and added `SportSignalCalculator` + `ProgressionService.applySport`, wired into the sport-log flow (`SportService.logSportSession` now returns a `levelUp` on `SportSessionResponse`). P3b also **lifted the robustness rate out of `gym()` into a shared `mezo.progression.robustness` node** and hardened `RunSignalCalculator` to load its log via an ownership-scoped finder. **P4 (profile read surface)** added the first `api/feature/progression/progression.yml` fragment (`GET /api/progression/profile`) + a switch-gated `ProgressionController` → `ProgressionService.getProfile` (athlete-level, 6 radar axes, streak, highlights — see below). The frontend's progression views stay mock-only until **P6** (the profile contract exists but has no FE consumer yet). So progression now contributes the `levelUp` field on the Train contract **and** its own profile fragment to `api/openapi.yml` / `frontend/src/data/_client/api.gen.ts`.

`feature/progression` ships:

- **Pure level math** — `ProgressionCurve` (`feature/progression/ProgressionCurve.java`): `xpThreshold(level)` (cumulative XP to BE at a level; `level ≤ 1 → 0`), `levelFor(cumulativeXp)` (highest reached level, capped at `MAX_LEVEL = 200` via the scan guard), `progressPct(cumulativeXp, level)` (within-level fill 0..100, with a `ceil ≤ floor` divide-by-zero guard returning 100). Tuned by `ProgressionProperties` (`mezo.progression.curve.{base,exp}`, defaults 100 / 1.6 — `@Validated` record, never `@Value`). No state, no DB. Spec'd by `ProgressionCurveTest` (plain JUnit, no Spring context).
- **In-memory perk catalog** — `PerkCatalog` (`feature/progression/PerkCatalog.java`): master content (skillKey + milestoneLevel → perk title/effect copy) loaded at startup from the classpath `content/progression-perks.json`, **no table / no `created_by`** (the per-user unlocks live in `perk_unlock`). Invalid content **fails startup fast** (`@PostConstruct validate`). IT: `PerkCatalogIT`.
- **Three new owned tables** (migration `db/changelog/1.0.0/script/202606251200_mezo-8e4_create_progression.sql`), all the usual UUID-PK + `created_by` FK→`app_user ON DELETE CASCADE` + soft-delete columns:
  - **`skill_progress`** — per-skill XP accumulator: `skill_key`, `skill_kind` (`ATHLETIC|MUSCLE`, DB CHECK), `cumulative_xp` (bigint), `current_level` (int, CHECK ≥ 1); idempotency key `uq_skill_progress_created_by_skill_key` (one row per skill per owner). Entity `entity/SkillProgressEntity.java` (`@UpdateTimestamp updated_at`).
  - **`level_up_event`** — the XP-grant ledger: one row per XP-granting workout regardless of whether a level was crossed. `source_type` (`GYM|SPORT|RUN`, DB CHECK), **`source_ref_id` UUID** — a *polymorphic* ref to the gym instance / sport / run session, **intentionally NOT an FK** (no single parent table to point at); `total_xp` (bigint); `payload` typed jsonb (`LevelUpResult` record via `@JdbcTypeCode(SqlTypes.JSON)`). **Idempotency key `uq_level_up_event_created_by_source` (`created_by, source_type, source_ref_id`)** — re-processing the same workout cannot double-grant. Entity `entity/LevelUpEventEntity.java`.
  - **`perk_unlock`** — per-user perk unlocks: `skill_key`, `perk_key`, `milestone_level` (CHECK ≥ 1); idempotency key `uq_perk_unlock_created_by_perk`. Entity `entity/PerkUnlockEntity.java`.
- **Repositories** — `repository/{SkillProgressRepository,LevelUpEventRepository,PerkUnlockRepository}.java`, date-less owned aggregates extending `JpaRepository` with bespoke `…CreatedByAndDeletedFalse` finders (same pattern as `GoalRepository`, §4b).
- **Test populators** — `support/populator/{SkillProgressPopulator,LevelUpEventPopulator}.java` (one per persisted aggregate; `perk_unlock` is exercised through the persistence IT directly). The three tables are in the `ResetDatabase` TRUNCATE list (§8). ITs: `feature/progression/{ProgressionPersistenceIT,ProgressionPopulatorIT}.java`.
- **Shared award tail (P3a)** — `service/ProgressionService.award(createdBy, sourceType, sourceRefId, deltas, kinds, label, durationMin, rpe) → LevelUpResult` (private; P3a extracted it from `applyGym`'s tail so every family reuses it). It runs **inside the caller's `@Transactional`** (no own `@Transactional`) and is the **family-agnostic** pipeline: **idempotency guard** at its top (`(sourceType, sourceRefId)` lookup on `level_up_event` → return the **stored `payload`** without re-awarding), then apply the per-skill XP `deltas`/`kinds` → `upsert skill_progress`, build gains + level-ups (`ProgressionCurve.levelFor`) + perks (unlock once per `perk_key` at every milestone strictly crossed 5/10/15/20/25/30 via `PerkCatalog.find`), recompute streak **robustness** (streak-only **absolute target** `streakWeeks*perWeekXp`, recomputed each apply → idempotent within a week; the rate is read from the family-agnostic `properties.robustness()` node — **lifted out of `gym()` in `mezo-lmox`** now that all three families feed it), and persist exactly one `level_up_event`. Streak comes from `feature/train/signal/TrainingStreakCalculator.streakWeeks` (implements the progression-side `RobustnessSource` port — keeps the package arrow train → progression one-way) — consecutive ISO weeks (Europe/Budapest) with ≥1 logged gym instance / sport / run, gathered via `WorkoutSessionRepository.findInstanceDates`, `SportSessionRepository...OrderByDateDesc`, `RunSessionLogRepository...OrderByDateDesc` (known v1 limitation: the week-key walk-back under-counts across ISO-year boundaries).
- **Gym-path XP engine (P2)** — `service/ProgressionService.applyGym(createdBy, GymSignal) → LevelUpResult` (`@Transactional`, `@Service`): builds the GYM-specific deltas from `ProgressionProperties.gym()` (muscle `volume/volumeUnit*volumeXpPerUnit`, integer; `max_strength = bestE1rm.intValue()*e1rmXpPerKg + prBonusXp` where the **v1 PR rule** awards the bonus only on the **first-ever weighted session** — no prior `max_strength` row; `strength_endurance = workSetCount*xpPerSet + bodyweightRepCount*xpPerRep`), then delegates the whole tail to `award(createdBy, "GYM", signal.instanceId(), …)`. Idempotent per `(GYM, instanceId)` (via `award`'s guard). IT: `feature/progression/service/ProgressionServiceIT` (3 tests: first-apply XP/level-up, idempotency, prior-row PR rule).
- **Run-path XP engine (P3a)** — `service/ProgressionService.applyRun(createdBy, RunSignal) → LevelUpResult` (`@Transactional`): builds the RUN deltas from `ProgressionProperties.run()`, then delegates to `award(createdBy, "RUN", signal.logId(), …)`. Idempotent per `(RUN, logId)` (every run-log POST is a new row, so each genuinely-distinct log grants once). All RUN skills are `ATHLETIC` — **run grants no muscle XP**. The branch keys off the signal's **kind** (`sprint`/`pyramid` → the sprint skill set; everything else → steady; **`pyramid` shares the skills but not the round arithmetic** — see the pyramid credit below): **sprint** → `sprint_speed = rounds*sprintXpPerRound`, `anaerobic_capacity = rounds*anaerobicXpPerRound`, `explosiveness = rpeActual*rpeXpPerPoint`; **steady** → `strength_endurance = min*steadyXpPerMin`, `aerobic_capacity = min*aerobicXpPerMin + (hrRecoverySec present ? hrRecoveryBonusXp : 0)`. A private `addAthletic` helper only adds positive deltas (no 0-XP gains), so any null/zero metric simply drops its skill. The signal comes from **`feature/train/signal/RunSignalCalculator.compute(createdBy, runLogId)`** (`@Component`): it reads the saved `run_session_log` row and **resolves the prescribed session's `kind`** by walking the owning `running_block`'s typed-jsonb `structure` (weeks → sessions, matched by `sessionKey`; **defaults to `steady`** when the block/structure/session is missing). `RunSignal` carries `(logId, kind, completedRounds, durationMin, rpeActual, sprintLandmark, hrRecoverySec, prescribedWorkSecs)` — `sprintLandmark` is plumbed but **not yet consumed** (reserved for a future sprint-scoring refinement). **Pyramid round credit (`mezo-d20.7.3`):** a pyramid's rungs are unequal by design (15/30/45/60/45/30/15 s), so paying the flat sprint rate per logged round misprices a partial run. `prescribedWorkSecs` carries the prescribed work-segment seconds **in plan order** — populated by `RunSignalCalculator` **only for `pyramid`**, whose segments enumerate every round individually (a sprint's segments are one work/rest template repeated `rounds` times, so it stays `null` there) — and the private `ProgressionService.pyramidRoundCredit` turns them into whole-round equivalents: `prescribedRounds × (work seconds of the first `completedRounds` rungs ÷ total prescribed work seconds)`, `completedRounds` capped at the prescription. Completing the whole pyramid therefore pays exactly the prescribed round count (the old flat result) — the fix only reprices partials, and **sprint scoring is unchanged**. Honest degradations: a legacy log with a `null` `completedRounds` (the pre-`mezo-d20.3.5` log sheet captured rounds for sprint only) earns **no** round XP rather than crashing or inventing credit — the RPE that *was* logged still pays — and a log whose prescription is unavailable falls back to flat per-round credit. Config: `ProgressionProperties.Run` (added to the existing `mezo.progression` record, not a new `*Properties`) — `sprintXpPerRound`/`anaerobicXpPerRound`/`steadyXpPerMin`/`aerobicXpPerMin`/`rpeXpPerPoint`/`hrRecoveryBonusXp` (defaults 25/15/4/5/6/30). ITs: `feature/train/signal/RunSignalCalculatorIT` (sprint kind + field mapping, default-to-steady, pyramid rungs carried / sprint rungs `null`) + `feature/progression/service/ProgressionRunIT` (sprint skills, steady skills, idempotency, pyramid full/partial/over-logged/legacy-null/no-prescription).
- **Sport-path XP engine (P3b)** — `service/ProgressionService.applySport(createdBy, SportSignal) → LevelUpResult` (`@Transactional`): builds the SPORT deltas from `ProgressionProperties.sport()`, then delegates to `award(createdBy, "SPORT", signal.sessionId(), …)`. Idempotent per `(SPORT, sessionId)`. All SPORT skills are `ATHLETIC` (no muscle XP). The branch keys off the **kind** (the `sport` column, `volleyball|cross|trx`): **volleyball** → `vertical_jump`/`agility`/`coordination = setsPlayed*xpPerSet`, `explosiveness = rpe*rpeXpPerPoint`, `aerobic_capacity = min*xpPerMin`; **cross** → `anaerobic_capacity`/`strength_endurance = rounds*xpPerRound`, `explosiveness`/`core_stability = rpe*rpeXpPerPoint`; **trx** → `core_stability`/`strength_endurance = rounds*xpPerRound`, `anaerobic_capacity = rpe*rpeXpPerPoint`, `mobility = min*xpPerMin`. (Volleyball scores off the logged `setsPlayed`, not `jumpCount`.) The signal comes from `feature/train/signal/SportSignalCalculator.compute(createdBy, sessionId)` (`@Component`) — an **ownership-scoped** load of the just-saved `sport_session` row, rounding its `BigDecimal` RPE HALF_UP to an `Integer`. `SportSignal` carries `(sessionId, kind, durationMin, setsPlayed, rounds, rpe)`. Config: `ProgressionProperties.Sport` (`xpPerSet`/`xpPerRound`/`xpPerMin`/`rpeXpPerPoint`, defaults 12/14/4/6). ITs: `feature/train/signal/SportSignalCalculatorIT` (cross metrics + RPE rounding + owner scoping) + `feature/progression/service/ProgressionSportIT` (volleyball/cross/trx skills, idempotency).
- **Gym finish + run log + sport log triggers (P2/P3a/P3b, switch-gated)** — both attach the `levelUp` **only** when the progression feature switch is on, each inside the host write's existing `@Transactional` (atomic with the save). The switch is read the house way — **no `@Value`/`Environment`**: a marker bean `feature/progression/ProgressionGate.java` (`@Component @ConditionalOnProperty(name = FeaturesConfiguration.PROGRESSION_SWITCH /* mezo.feature.progression.enabled */, havingValue = "true")`, no `matchIfMissing`) exists **only** when the property is `true`; the host services inject it via `ObjectProvider<ProgressionGate>` and `getIfAvailable() != null` ⇔ switch on. **Gym:** `WorkoutService.finishWorkout` runs `GymSignalCalculator.compute` → `applyGym` → `LevelUpResultMapper.toDto` → `setLevelUp` on `WorkoutInstanceResponse` (the shared `toInstanceResponse` is **unchanged**, so `startWorkout`/resume never carry a `levelUp`; idempotent on the instance id, so a re-finish does not double-award). **Run:** `RunningService.logSession` runs `RunSignalCalculator.compute(userId, savedLogId)` → `applyRun` → `LevelUpResultMapper.toDto` → `setLevelUp` on `RunSessionLogResponse` (only on the POST/save path, never the GET list; idempotent on the saved log id). **Sport:** `SportService.logSportSession` runs `SportSignalCalculator.compute` → `applySport` → `LevelUpResultMapper.toDto` → `setLevelUp` on `SportSessionResponse` (POST only, never the GET list; idempotent on the saved session id). Contract ITs: `feature/train/WorkoutFinishLevelUpApiIT` (gym finish → `levelUp` source GYM, totalXp > 0, a `chest` gain) + `feature/train/RunSessionLevelUpApiIT` (sprint run log → `levelUp` source RUN, a `sprint_speed` gain); sport is covered service/contract-side by `feature/train/SportServiceIT` (volleyball log → `levelUp` source SPORT, cross kind+rounds persisted) + `feature/train/SportContractIT` (cross without volleyball fields → 201 + `levelUp`, bad `sport` → 400).
- **Profile read surface (P4)** — `controller/ProgressionController.java` (`@RestController implements ProgressionApi`, gated by `@ConditionalOnProperty(PROGRESSION_SWITCH, havingValue = "true")` — the **first** switch-gated controller; absent → `GET /api/progression/profile` 404s) → `ProgressionService.getProfile(createdBy)` (a pure read, **no** `@Transactional`). It reads all `skill_progress` rows and aggregates over the fixed `ProgressionTaxonomy` (11 non-robustness athletic + robustness; 13 muscle tokens): each `SkillLevel` derives a level (**missing row → level 1**) + `progressPct`; **athleteLevel** = mean of the 11 non-robustness athletic levels (1-decimal, `null` when the user has **zero** rows → ghost); **6 fixed radar axes** (Robbanékonyság/Sebesség/Állóképesség/Mozgékonyság/Koordináció regroup the 11; **Erő** blends `max_strength` with the muscle-level mean via `mezo.progression.radar.strength-muscle-blend`, default 0.5); **streakWeeks** from `TrainingStreakCalculator` (via the `RobustnessSource` port); **highlights** = best existing athletic (excl. robustness) + best muscle by `(level, cumulativeXp)`. Contract: the first `api/feature/progression/progression.yml` fragment (tag `Progression` → `ProgressionApi`; `getProfile` → `ProgressionProfileResponse {athleteLevel?, streakWeeks, athletic[], muscle[], radarAxes[], highlights}`). ITs: `feature/progression/service/ProgressionProfileServiceIT` (ghost, athlete-level over the 11, Erő muscle-blend, robustness excluded) + `controller/ProgressionProfileApiIT` (HTTP ghost + 401) + `controller/ProgressionProfileSwitchOffApiIT` (switch-off → 404).
- **FE consumer (P5, `mezo-te8k` — frontend-only)** — the `levelUp` on the three finish responses is no longer discarded: the gym `finishWorkout` + the sport/run log mutations now **forward the response**, and all three flows present it through a **single host** — `LevelUpProvider`/`useLevelUp()` (`frontend/src/features/progression/`, mounted once in `AppLayout`) renders the full-bleed `LevelUpScreen` overlay (self-portaled into `.phone-screen`, above the TabBar; hand-rolled CSS keyframes, rAF count-up, **`prefers-reduced-motion` first-class** via `useReducedMotion`). The gym overlay layers over the existing `WorkoutComplete` recap (revealed on `Tovább`). Because the backend sends `gain.name = <raw skillKey>` and `gain.icon = null`, the **FE owns the HU display name + emoji** (`levelUpMeta.ts`; muscle names reuse Train's `MUSCLE_LABELS`). **Mock mode** can't compute a payload, so the no-op finish/sport/run mutations return deterministic seeded `LevelUpResult` fixtures (`frontend/src/data/progression/progressionMock.ts`). See `docs/features/train.md` §2 (COMPLETE phase) + §5 (the level-up seam).
- **FE profile cards (P6, `mezo-xje5` — frontend-only; completes the `mezo-8e4` epic)** — the P4 `GET /api/progression/profile` now has an FE consumer: a dual-mode `useProgressionProfile()` hook (`frontend/src/data/progression/progressionHooks.ts` + `data/progression/progressionApi.ts`) feeds two cards in the Me/Profile view (`frontend/src/features/me/components/{AthleticRadarCard,MuscleLevelsCard}.tsx`) — a hand-rolled SVG hexagon radar of the 6 server-computed `radarAxes` + athlete-level/best/streak stats, and a top-N muscle-levels card + reserve note. Both ghost on `athleteLevel == null` (no XP / switch-off 404); mock returns a seeded `progressionProfileMock`. The radar reuses the P5 `useReducedMotion`. **With P6 the `mezo-8e4` progression epic (P1 domain → P2 gym → P3a run → P3b sport → P4 profile API → P5 level-up overlay → P6 profile cards) is fully shipped.** See `docs/features/me.md` §2 (`Profil`) + §3.

### 4f. The gamification ledger domain (`feature/gamification`, `mezo-huzd`) — the account-wide coin/streak/title backend

The **account-wide** counterpart to §4e's per-skill progression: coins, a daily streak + saver, and a title shop, surfaced by the shared `AppHero` header (`mezo-k7rn`, frontend-first — real backend landed here). Full user-facing/domain narrative lives in [`growth.md`](growth.md) "Account progression"; this is the backend-architecture summary.

- **Three owned tables** (migration `202607241100_mezo-huzd_create_gamification.sql`): `gamification_profile` (one row/owner — `coins`, `streak_days`, `streak_savers`, `last_streak_date`, `equipped_title_key`, `account_level`), `coin_event` (append-only ledger — `reason`, `amount`, `occurred_on`, idempotency key `(created_by, reason, source_ref_id)`), `owned_title` (shop purchases only — ladder titles are level-derived, never a row; `acquired_at` via `@CreationTimestamp`). A second migration (`202607241130_mezo-huzd_level_up_event_occurred_on.sql`) adds `occurred_on` (business date) to the EXISTING `level_up_event` table (§4e) — set explicitly by every `ProgressionService.award(...)` call (GYM/RUN/SPORT stamp `LocalDate.now()`; QUEST/ACTIVITY/HABIT carry their own row-borne date), never defaulted.
- **`AccountProgressPort`** (`feature/progression/AccountProgressPort.java`) — a one-directional hook progression exposes (gamification implements, never the reverse); `ProgressionService.award(...)`'s shared tail (§4e) calls it via `ObjectProvider` **once per newly-created `level_up_event`**, never on the idempotent replay path. `feature/gamification/service/GamificationAccountAdapter` implements it (`Propagation.MANDATORY` — runs inside the caller's transaction, so a rollback of the XP grant rolls the coin/streak side back too) and fans the event into streak rollover + per-source coin awards (quest completion, the day's 3rd-quest bonus, every account level crossed).
- **`GamificationService`/`GamificationController`** (both `@ConditionalOnProperty(mezo.feature.gamification.enabled)`) serve the reads (`getProfile`, the day Harvest aggregate) and the three purchase mutations (buy/equip title, buy streak saver) — see the endpoint table in §4c.
- **`TitleCatalog`** (`content/gamification-titles.json`, fail-fast startup load, the `PerkCatalog` idiom) — a byte-for-byte port of the FE's `titleCatalog.ts`; the two must be edited together (no shared source yet).
- **`GamificationProperties`** (`mezo.gamification`, `@ConfigurationProperties`) — saver price/cap + every coin-award amount, never hardcoded.
- **Demodata** (`GamificationDemoData`, `@Profile("demodata")`, Order 135) seeds a 240-coin / 6-day-streak profile matching the FE mock's story, with the equipped title derived from the owner's actual seeded skill XP (never hardcoded), plus one dated `coin_event` so a demo Harvest read isn't empty.

### 4g. Push notifications — `techcore/webpush` + `feature/notification` (N1+N2+N3 all shipped, `mezo-h4wp.6.1`/`.6.2`/`.6.3`)

The delivery-infra counterpart to the proactive epic (§ `Proactive` row above): a Web Push (RFC 8030/8291/8292) stack the backend owns end to end, delivering the proactive prose + time-anchored reminders to the device with the app closed. **Full architecture, data model, the 11-category catalog, copy rules, dispatcher gotchas, and status are in [`_platform-notifications.md`](_platform-notifications.md) — not restated here**, only the facts this doc's own inventory needs:

- **`techcore/webpush`** (infra, not a feature package) — `WebPushProperties`/`VapidSigner`/`Aes128GcmEncryptor`/`WebPushClient`/`WebPushResult`/`WebPushSubscriptionKeys`, in-house (zero new Maven dependencies), correctness pinned by the RFC 8291 §5 test vector — see [ADR 0014](../decisions/0014-own-webpush-implementation.md). **`WebPushClient` is the app's third outbound `RestClient`**, after `OffClient` (Pantry P6, §4c) and `WebPageClient` (Pantry P8, §4c).
- **`feature/notification`** — N1's `PushSubscriptionService`/`PushSender`, plus N2/N3's `NotificationPrefService`/`NotificationScheduleService`/`DueEvaluator`/`AnchorResolver`/`NotificationDispatchJob`/`PushDispatchExecutor`, all behind one `NotificationController implements NotificationApi` (one interface per OpenAPI tag — no per-slice split). Four tables: `push_subscription` (N1), `notification_pref` + `push_log` (N2), `notification_schedule` (N3) — migrations `202607291000_mezo-h4wp.6.1_*`/`202607291400_mezo-h4wp.6.2_*`/`202607291500_mezo-h4wp.6.3_*`.
- **`NotificationDispatchJob` is the app's first per-minute `@Scheduled` cron.** Because the Spring scheduler pool is size 1 (shared by every `@Scheduled` method), it only does DB-only due-computation on the scheduler thread and hands the actual outbound send to `PushDispatchExecutor`, a **separate** `@Async` bean — inlining the send would starve every other cron (nightly summary, dawn briefing, habit close). See the platform doc §9 for why this is the single most load-bearing gotcha in the feature.
- **Three new webpush message codes**, a deliberate client-vs-server split: `WEBPUSH_KEY_INVALID` (400 — reserved for material that arrived from a client, e.g. a subscription's `p256dh`), `WEBPUSH_SIGN_FAILED` (500 — VAPID config or our own DER/JOSE encoding), `WEBPUSH_ENCRYPT_FAILED` (500 — our own RFC 8188 framing). Plus **`NOTIFICATION_UNKNOWN_CATEGORY`** (400, N2/N3) — one code enforcing two different rules depending on the endpoint (any-unrecognized-key on `pref`; unrecognized-**or**-backend-native on `schedule`, the client-cannot-own-a-backend-minute security boundary).
- **Switches:** `mezo.feature.notification.enabled` (whole `/api/notification/*` surface) + `mezo.techcore.cron.notification-dispatch-job.enabled` — both **on**; the dispatcher is live.
- **Real-world delivery confirmed:** a real Web Push reached Daniel's iPhone from the k3s backend on 2026-07-29 (N1's exit criterion) — the platform path is proven end to end, not only unit-tested.

---

## 5. Integrations

This is the most load-bearing section — every seam, bidirectionally, with the crossing type.

**Contract ↔ Backend (generate-sources seam).** `api/openapi.yml` → `io.mrkuhne.mezo.api.controller.<Tag>Api` (interfaces) + `io.mrkuhne.mezo.api.dto.*` (DTOs, under `target/generated-sources/openapi`, gitignored). Controllers `implements <Tag>Api`; services and mappers consume `api.dto` types directly (`LogWeightRequest`/`WeightLogResponse`, `MesocycleResponse`, …). **Crossing type:** every `*Request`/`*Response` schema. **Drift = compile error.** Generator config (`backend/pom.xml` lines ~175-215): `generatorName=spring`, `apiPackage=io.mrkuhne.mezo.api.controller`, `modelPackage=io.mrkuhne.mezo.api.dto`, `inputSpec=../api/openapi.yml`; `useSpringBoot4=true`, `interfaceOnly=true`, `skipDefaultInterface=true`, `useResponseEntity=false`, `useTags=true`, `openApiNullable=false`, `annotationLibrary=none`, and `additionalModelTypeAnnotations=@lombok.Builder @lombok.NoArgsConstructor @lombok.AllArgsConstructor` (so DTOs are builder-able — `LogWeightRequest.builder()` in tests, `WorkoutInstanceResponse.builder()` in services; generated enums expose `fromValue(...)`, used in mappers e.g. `MesocycleResponse.StatusEnum.fromValue(...)`).

**Contract ↔ Frontend (generate seam).** `api/openapi.yml` → `frontend/src/data/_client/api.gen.ts` via `openapi-typescript`. `lib/<x>Api.ts` clients use the types: request bodies are written `... satisfies <Request>`, responses typed with the generated response type and structurally assigned to the hook layer's domain types (e.g. `WeightLogResponse[]` is assignable to `WeightEntry[]`, checked by tsc). Where a domain type is stricter than the contract, the seam uses an **explicit documented cast** (e.g. `sleepApi.list` casting `SleepLogResponse[] as SleepEntry[]` with a TODO/bd note) — never a silent re-widen of `apiFetch<T>`. **Crossing type:** the same schema names, surfaced as TS types.

**Backend ↔ DB.** Entities ↔ Liquibase DDL stay in lockstep — every constraint mirrored both ways, enforced at startup by Hibernate `ddl-auto: validate` (Liquibase owns the schema, Hibernate never mutates it; `open-in-view: false`). jsonb columns ↔ typed records via `@JdbcTypeCode`.

**Auth ↔ every protected feature (the universal seam).** `AuthService` mints the JWT with `subject = app_user.id`; `CurrentUserId` (`techcore/security/CurrentUserId.java`) delegates to `CurrentUser` (`feature/auth/service/CurrentUser.java`), which reads `SecurityContextHolder`, expects a `Jwt` principal, loads the account, rejects a `DISABLED` status with 403, and returns `UUID.fromString(jwt.getSubject())` (throws `AUTH_TOKEN_MISSING`/401 for a missing/invalid token). This single bean is injected into every controller and feeds `OwnedEntity.createdBy`, stamped server-side on every write. `OwnerProperties` (`mezo.auth.*`) feeds both `SecurityConfig` (the HS256 JWT secret) and `OwnerSeedData`. The frontend obtains the token via `authApi.login`/`register` (`data/auth/authApi.ts`) → `tokenStore` (`data/_client/tokenStore.ts`, `localStorage`) → injected by `apiFetch`. The test framework's `ApiIntegrationTest.ownerAuthHeaders()`/`registerUser()`, `ResetDatabase`, and `UserPopulator` all depend on the demodata owner existing → ownership/auth is the seam every other seam rides on. Full detail: [`_platform-auth-security.md`](_platform-auth-security.md).

**Frontend hook layer ↔ data clients.** `frontend/src/data/hooks.ts` is the single FE↔data boundary; it re-exports `useTrain` from `trainHooks.ts` and `useRunning` from `runningHooks.ts` (so consumer import paths stay `@/data/hooks`). `isMockMode()` (`data/_client/mode.ts`) is the toggle. (The barrel dropped its dead `useInsightsTeaser` re-export in the Napív S8 cleanup, `mezo-mifi` — the hook was orphaned when Today's `InsightsTeaser` was removed; see [today.md §9](today.md).) **Crossing contract:** the hook's *own* return shape (e.g. `WeightEntry`, `SleepEntry` from `frontend/src/data/types.ts`) — stable and hand-written, decoupled from the generated DTOs by the `*Api.ts` adapter layer. **Every barrel line now has a contract behind it.** The gamified-header slice (`mezo-k7rn`) started `useGamification`/`useTitles`/`useGamificationActions` (`@/data/gamification/gamificationHooks`) as a **frontend-first, mock-only** account-progression domain (XP/level/coins/streak/titles) with no `api/feature/gamification` fragment — its real-mode interim derived only the account level from the *existing* `GET /api/progression/profile`, with coins/streak/titles ghost. The backend fast-follow **`mezo-huzd`** closed that gap: `api/feature/gamification/gamification.yml` now backs the whole domain (§4f), the interim (`fetchDerivedGamification`) is retired, and the barrel's fourth export, **`useGamificationDay`**, rides the same real contract (see [growth.md](growth.md)).

**Error contract ↔ both sides.** The hand-written `techcore` `SystemMessage` (exception layer) and the generated `api.dto.SystemMessage` (wire contract from `common-schemas.yml`) are **two deliberately separate types kept field-compatible** by the contract ITs (`assertHasFieldError`/`assertHasRequestError` match on code/type/field, never resolved text). The FE has its own `SystemMessage` interface + `ApiError` in `data/_client/api.ts`. All three must agree on the field set — the contract IT is the guard.

**Train-internal seams (largest consumer).** `TrainController` fans one `TrainApi` out to seven services (`TrainService`, `WorkoutService`, `SportService`, `GymScheduleService`, `ExerciseCatalogService`, `ExerciseRecordService`, `RunningService`). `GymScheduleService` maintains the recurring weekly gym-time slots (`gym_schedule_slot`, `GET/PUT /api/train/gym-schedule`, full-replace) exactly like `SportService`'s sport-schedule methods. The `workout_session` table is reused for both **template days** (`templateSessionId == null`) and **workout instances** (`templateSessionId` set, `date`+`status` populated) — the discriminator column is the seam between the planning model (`TrainService`) and the execution model (`WorkoutService`). `ExerciseRecordService` aggregates over logged `exercise_set` rows; the exercise catalog (`exercise_catalog`, loaded by `ExerciseCatalogLoader`) is referenced *optionally* by `exercise.catalog_id` (unknown catalogId → 400 FIELD `VALIDATION_INVALID_VALUE`, never a raw FK 500). The catalog is now **writable** (`mezo-52zg`, `202607082000_mezo-52zg_catalog_write.sql` adds `created_by`/`is_deleted`/`video_url`): `ExerciseCatalogService` adds owner-scoped `POST/PUT/DELETE /api/train/exercises` (user-authored rows — editing/deleting a master `created_by null` row → **409 `CATALOG_MASTER_READONLY`**) + `PUT /api/train/exercises/{id}/video` (attach/clear a demo video on ANY row incl. master) and, since `mezo-8xdl.1` (`202608052135_mezo-8xdl.1_exercise_catalog_images.sql` adds `image_start_url`/`image_end_url`), the symmetric `PUT /api/train/exercises/{id}/images` — both media writers are ownership-free because attaching media is not authoring content. The non-clobbering `ExerciseCatalogLoader` upserts master content by slug without touching user rows or overwriting a user-set video/image, and `CatalogMediaResolver` (renamed from `CatalogVideoResolver`) resolves video + both image frames onto `TodayExercise`/`GymExercise` in ONE batched catalog fetch. Hypertrophy Drive (`mezo-dhdr`) adds one more Train-internal helper — the switch-gated (`mezo.feature.hypertrophy-drive.enabled`, marker bean `HypertrophyDriveGate`) `SetRecommendationService` double-progression engine the workout services call to fold server-derived `prescribedSets`+`rationale` onto `GET /workouts/today` (the `exercise` row became a set-recipe; tunables under `mezo.hypertrophy.*`). The fix-zárás closing block (`mezo-z2ul`) adds one more: the switch-gated (`mezo.feature.closing-block.enabled`, marker bean `ClosingBlockGate`) `ClosingBlockService`, an idempotent lazy ensure `getToday` runs to append the configured closing exercises (`mezo.closing-block.exercises`, catalog-slug references) to every non-empty template day of the active meso. The session-flow fixes (`mezo-cd8s`) grow `api/openapi.yml` further: a read endpoint `GET /api/train/workouts/{id}` → `WorkoutDetailResponse`/`WorkoutDetailExercise` (the done-day review source; foreign/template-row id → 404), a `completedWorkout` field on `WorkoutTodayResponse`, and an **always-on** `WorkoutAutoCloseService` (`getToday`'s first statement settles stale `active` instances). The same change **flips the gym done-state from ≥1-logged-set to `status = 'completed'`** across `findDoneInstanceDates`/`findDoneInstancesBetween`, so every consumer (`weekDoneDates`, Insights `listWorkouts`, quests, the discipline trait, the companion `get_training_log` tool's `scope=gym`) reads completed-only. On the proactive side it adds the additive nullable structured targets (`targetWeightKg`/`targetReps`/`targetSets`/`targetRir`) to `ChallengeResponse` (`proactive.yml`, MapStruct-mapped by name) for the FE pre-finish outcome preview. See the Train feature doc for the full fan-out.

---

## 6. How to use it (consume)

Two consumer surfaces.

**(a) Frontend — use an existing backed hook.** Import the hook from the single boundary, never the `*Api.ts` client directly:

```ts
import { useWeight } from '@/data/hooks'

function WeightPanel() {
  const { weightLog, logWeight } = useWeight()   // dual-mode: real or mock, transparently
  // weightLog: WeightEntry[]  (the hook's own shape, not the generated DTO)
  return <button onClick={() => logWeight({ date: today, weightKg: 82.4, note: null })}>Mentés</button>
}
```

The hook hides `isMockMode()` entirely; the component never sees `apiFetch` or a DTO. In real mode the mutation hits `POST /api/biometrics/weight` and invalidates the query; in mock mode it emulates the server via `setQueryData`.

**The barrel is app-wide, not per-feature** — a hook is exported from `@/data/hooks` regardless of which feature owns its endpoint, and several are consumed by screens outside their owning domain (newest: **`useFeedback`**, `mezo-b3pp.15` — a companion-owned surface mounted by four Insights pages *and* Today's message sheet). That is why `frontend/src/data/hooks.ts` moves on almost every slice and shows up as a `key_files` entry on many docs. Hook-shape rules, `useDualQuery` and the barrel's full inventory live in [`_platform-data-layer.md`](_platform-data-layer.md).

**(b) Backend — call a feature from another feature.** Inject the *service* (not the controller), and resolve ownership from `CurrentUserId`:

```java
@Service
@RequiredArgsConstructor
class SomeService {
    private final WeightLogService weightLogService;
    private final CurrentUserId currentUserId;
    // ... weightLogService.list(currentUserId.get()) ...
}
```

Constructor injection only (Lombok `@RequiredArgsConstructor`), never field injection. The security principal is resolved via `currentUserId.get()` — it is **never** a contract/method parameter passed from the client.

---

## 7. How to extend it

The concrete recipe to add a new **backed feature or endpoint** (this is also the path to give Fuel/Insights/People a backend). Read the referenced `docs/references/*.md` *before* writing each layer — they are mandatory house standards.

1. **bd issue + `feat/<topic>` branch** (one issue/branch per change; merge `--no-ff` into main).
2. **Contract first** — add/extend `api/feature/<name>/<name>.yml`; **append it to `api/generate/merge.yml` `inputs:`**; run `cd api/generate && npm run generate:api`; commit `api/openapi.yml`. Follow `docs/references/api_contract_conventions.md` (schema/operationId/tag naming; spec-driven validation; `pattern` not `enum` on requests; `SystemMessageList` on every error; `security: []` for public endpoints). **Response codes are load-bearing** — with `useResponseEntity=false`+`interfaceOnly`, the controller returns the bare DTO and the 2xx status comes from the *first 2xx code* in the spec (201 vs 200 vs 204).
3. **Migration** — new Liquibase changeset named `{YYYYMMDDHHMM}_{bd-id}_{desc}.sql` (mezo uses the **driving bd id**, not spec-kit `F{NNN}`), listed in `db/changelog/1.0.0/1.0.0_master.yml`. UUID PK, `created_by` FK→`app_user ON DELETE CASCADE`, soft-delete columns, explicit named constraints (`pk_/fk_/uq_/ck_/idx_`). Seed data is Java `@Profile("demodata")`, **never SQL**. Follow `docs/references/liquibase_conventions.md`.
4. **Entity** — `extends OwnedEntity`, UUID `@Id`, `@SQLDelete`+`@SQLRestriction`, mirror every DB constraint; jsonb → typed record via `@JdbcTypeCode(SqlTypes.JSON)`. Follow `docs/references/java_package_structure.md`.
5. **Repository** — `extends OwnedRepository<E>` if a date-ordered owned list suffices (gives `findAllOwned(createdBy)` for free — **requires an `e.date` field**; date-less aggregates need a bespoke finder, per the Javadoc). Else derived queries → JPQL → native (last resort). Follow `docs/references/spring_patterns.md`.
6. **Service** — `@Service @RequiredArgsConstructor`; **reads have no `@Transactional`, writes get method-level `@Transactional`**; stamp `createdBy` from `currentUserId.get()`; ownership gate returning **404** for missing *or* foreign rows (no existence leak); throw `SystemRuntimeErrorException` with a `messages.properties` code (add the code). Follow `spring_patterns.md` + `docs/references/error_handling.md`.
7. **Mapper** — MapStruct `@Mapper(componentModel="spring")`, entity→`api.dto`; `@Mapping` for renames (e.g. `weightKg`→`value`), `default` methods for type bridges (`Instant`→`OffsetDateTime`, enum `fromValue`). **Never hand-write a boundary DTO.**
8. **Controller** — `implements <Tag>Api`, inject the service + `CurrentUserId`, delegate. **No** `@RequestMapping`/`@PostMapping`/`@Valid`/`@ResponseStatus` on the impl — all of that comes from the generated interface.
9. **Config** — any tunable → `application.yml` under `mezo:` + a `@Validated *Properties` record (never `@Value`); feature toggles → a `FeaturesConfiguration` constant + `@ConditionalOnProperty` at the bean boundary. Follow `docs/references/configuration_conventions.md`.
10. **Tests (same change)** — add a `*ServiceIT` (`extends AbstractIntegrationTest`, `@Transactional`) and a `*ContractIT` (`extends ApiIntegrationTest`, HTTP round-trip through the generated interface); **add the new owned table to the `ResetDatabase` TRUNCATE list and add a `*Populator` for the new aggregate — in this same commit**. Assert via the verb helpers + `assertHas*Error`. Follow `docs/references/testing_standards.md` + `integration_test_framework.md`.
11. **Frontend** — `cd frontend && pnpm generate:api`; add a `lib/<name>Api.ts` client (request bodies `satisfies <Request>`, typed responses, explicit documented cast if the domain type is stricter); wire a **dual-mode** hook in / re-exported from `data/hooks.ts` (`isMockMode()` branch, `initialData` for mock, `invalidateQueries` for real). **Both test modes green + `pnpm build`.**
12. **Docs** — ADR in `docs/decisions/` for any direction change; update `docs/infrastructure/` for infra; per-feature spec/plan in `docs/superpowers/specs|plans/`; write the feature doc in `docs/features/`. Work isn't done until `docs/` reflects it.

To extend a **Train sub-feature** specifically, see the Train feature doc — the contract fragment is the single big `api/feature/train/train.yml`, and the new endpoint is wired onto the existing `TrainController` fan-out.

---

## 8. Testing

References: `docs/references/testing_standards.md` + `integration_test_framework.md`. Integration-first; AssertJ only; **no mocks / `@MockBean` / H2**; Java-based data via populators (no SQL test data); `test{Method}_should{Result}_when{Condition}` naming.

- `support/AbstractIntegrationTest.java` — `@SpringBootTest`, imports `TestcontainersConfiguration`, `DatabasePopulator`, `UserPopulator`, `TrainPopulator`, `ResetDatabase`; `@BeforeEach` runs `ResetDatabase.resetExceptMasterData()`. Service-level subclasses add their own `@Transactional` for rollback.
- `support/ApiIntegrationTest.java extends AbstractIntegrationTest` — `@ActiveProfiles("demodata")`, `RANDOM_PORT`, `TestRestTemplate`. **Not `@Transactional`** (server commits in its own tx; cleanup via `ResetDatabase`). Provides `ownerAuthHeaders()` (logs in via `/api/auth/login` with credentials from `OwnerProperties`, never hardcoded); verb helpers `getForBody/getForList/postForBody/putForBody/deleteAndExpect/exchangeForBody` where the **expected status is always a param and always asserted**; `exchangeForResponse` for header inspection (CORS); and `assertHasFieldError(body, field, code)` / `assertHasRequestError(body, code)` that parse the `List<SystemMessage>` JSON and match on **code/type/field, never resolved text** (codes are the contract). (SB4 → Jackson 3, `tools.jackson.databind.ObjectMapper`.)
- `support/ResetDatabase.java` — one `TRUNCATE ... CASCADE` over all owned domain tables (now incl. `goal_plan_link` + the three progression tables `skill_progress`, `level_up_event`, `perk_unlock`) + `DELETE` of non-owner users/profiles. **Growth rule (mandatory): every new owned table is added here in the same change; `exercise_catalog` is master data and must NOT be truncated.**
- `support/populator/{UserPopulator,TrainPopulator,RunningPopulator,…,SkillProgressPopulator,LevelUpEventPopulator}.java` — one `<Aggregate>Populator` per aggregate; `DatabasePopulator` is the facade. **Growth rule: each new aggregate gets its own populator in the same change.** (`mezo-8e4` adds `SkillProgressPopulator` + `LevelUpEventPopulator`.)
- DB target: fixed `mezo_test` compose DB by default; throwaway Testcontainers via `-Dmezo.test.use-testcontainers=true`. Surefire also runs `*IT.java` (Failsafe deliberately unconfigured to avoid double execution).
- Canonical examples: `feature/biometrics/BiometricsContractIT.java` (round-trips weight POST/GET, asserts a 400 FIELD error, asserts check-in upsert); `feature/train/ProvenanceRoundTripIT.java` (proves the typed-jsonb envelope survives a DB round-trip).

**Commands:**

```bash
# Backend (compose Postgres up first)
cd backend && docker compose up -d
./mvnw clean test                                   # ITs vs fixed mezo_test DB — ALWAYS use clean (Lombok+MapStruct incremental is flaky)
./mvnw clean test -Dmezo.test.use-testcontainers=true   # throwaway PG (CI / no compose)

# Frontend — BOTH modes must be green
cd frontend
pnpm test                          # REAL mode (default)
VITE_USE_MOCK=true pnpm test       # MOCK mode
pnpm build                         # tsc -b && vite build

# Contract regen
cd api/generate && npm run generate:api    # merge fragments -> api/openapi.yml
cd frontend && pnpm generate:api           # regenerate src/data/_client/api.gen.ts
# backend Java DTOs/interfaces regenerate automatically in mvn generate-sources
```

---

## 9. Decisions, gotchas & deferred

- **Contract-first is the rule, not a guideline.** Never hand-write boundary DTOs; never re-add validation the spec can express. Both generated artifacts (`api/openapi.yml`, `frontend/src/data/_client/api.gen.ts`) are committed and regenerated together in one green commit; breaking changes are allowed *within* Phase 2 since FE+BE ship together. Spec → `docs/superpowers/specs/2026-06-10-phase2-backend-design.md`; convention → `docs/references/api_contract_conventions.md`.
- **SSE endpoints in the contract-first flow (V0.4 precedent, `mezo-fnnq.4`; the live `tool` event added mezo-280).** A streaming operation **stays in the contract fragment** (single source of truth; its event payload schemas — `StreamDelta`, `StreamToolCall`, `StreamError` — generate both FE types and backend `api.dto` models) but goes under a **dedicated tag** (`CompanionStream`), and the generated `<Tag>Api` interface is **deliberately left unimplemented** — an interface no bean implements contributes no mappings, so it is inert. The controller is **hand-written** (`CompanionStreamController`: own `@PostMapping(produces = TEXT_EVENT_STREAM_VALUE)` + `@Valid @RequestBody` on the generated request DTO, returning `Flux<ServerSentEvent<Object>>` — MVC adapts it via reactor-core, which Spring AI already brings) because the generator cannot express a reactive SSE return type. Both ArchUnit guards carry a **documented allowlist entry** (`ArchitectureTest.HAND_WRITTEN_CONTROLLER_ALLOWLIST`; the LLM fake's forced-failure sentinels are likewise allowlisted in the raw-exception rule). Every event `data:` line is JSON; pre-stream failures (400/401/404) surface as ordinary `SystemMessageList` JSON — clients send `Accept: text/event-stream, application/json`. This is the ONE sanctioned exception to "controllers implement the generated interface"; reuse the same shape for any future streaming endpoint.
- **`enum` vs `pattern` on request fields** — invalid enum → 500 (Jackson), invalid pattern → 400 (bean validation). Use `pattern` for request enums (live in `SportScheduleSlotInput`, both `kind` and `sport`).
- **`@NotBlank` gotcha** — no OpenAPI equivalent; use `required` + `minLength: 1`. An empty string then yields `VALIDATION_INVALID_VALUE` (Size), not `VALIDATION_REQUIRED_FIELD` (null/missing only).
- **Cross-field validation lives in the service, not the spec** — a `targetDate >= startDate` rule can't be expressed in OpenAPI (no cross-field constraint), so `GoalService.applyUpsert` guards it in code: inverted window → 400 FIELD `VALIDATION_INVALID_VALUE` on `targetDate` (G4b, `mezo-b0k`). It pre-empts a 500 in `GoalTimelineService` (a negative window length); §4d. Same principle for any other inter-field invariant — guard at the write boundary, throw a FIELD `SystemMessage`.
  - **The rule extends to a DB CHECK the spec also cannot express (Phase 5 W4.1, `mezo-b3pp.15`).** `message_feedback` carries `ck_message_feedback_reason` (`reason is null or verdict = 'down'`) — a cross-COLUMN constraint, so neither a `pattern` in the fragment nor a `@Pattern` on the entity can see it (both look at one field). Left to the database it would surface as a `DataIntegrityViolationException` → **500**, which is not an answer to a request the client got wrong. `MessageFeedbackService.put` therefore checks it first and throws a 400 `FEEDBACK_REASON_REQUIRES_DOWN` (new code in `messages.properties`, Hungarian message like every other), with the CHECK kept purely as the backstop for anything that reaches the table by another path. **Enforce a cross-field invariant in BOTH places when a DB constraint exists** — the constraint protects the data, the service check produces the honest status code.
- **Two `SystemMessage`s** (hand-written `techcore` vs generated `api.dto`) are kept field-compatible by contract ITs — **do not unify them.**
- **`OwnedRepository.findAllOwned` requires an `e.date` field** — date-less aggregates need bespoke finders. The `deleted = false` JPQL clause is intentionally redundant with `@SQLRestriction` — keep both, the comment says so explicitly.
- **Soft delete + ownership gate** → "not found" and "not yours" are intentionally indistinguishable (both 404) — no existence leak.
- **Always `./mvnw clean`** (Lombok + MapStruct incremental compile is flaky).
- **`useResponseEntity=false` + `interfaceOnly` + `skipDefaultInterface`** → the spec's response codes are load-bearing (the first 2xx code becomes the HTTP status).
- **One controller per tag — the `skipDefaultInterface` trap (Phase 5 W1.4, `mezo-b3pp.4`).** `skipDefaultInterface=true` means the generated `<Tag>Api` interface has NO default methods, so it can be `implements`ed by exactly one Spring bean — Spring wires each unimplemented interface method to whichever single controller declares it, and two controllers each implementing a subset of the SAME tag's interface simply doesn't compile (both would have to implement ALL of it). Adding an endpoint to an EXISTING tag therefore means implementing it on the EXISTING controller for that tag, not spinning up a new one, however unrelated the new endpoint feels. Concretely: the three `POST/GET /journal/decision*` operations share the `Journal` tag with the pre-existing journal CRUD, so they live in `JournalController` (delegating to the separate `DecisionService`) rather than a new `DecisionController` — see the code comment at `JournalController.java` (right above `createDecisionEntry`). The fix if a new endpoint genuinely wants its own controller is to give it its own tag in the contract fragment (the `CompanionStream` precedent two bullets up), not to fight this constraint. **Worked example of taking that exit deliberately (Phase 5 W4.1, `mezo-b3pp.15`):** the feedback surface belongs to the companion feature but got its own fragment `api/feature/companion-feedback/companion-feedback.yml` + tag `CompanionFeedback` → `CompanionFeedbackApi` → its own thin `CompanionFeedbackController`, precisely so `CompanionController` did not have to absorb a third responsibility beside conversations/facts/patterns/memory. Fragment ≠ feature package: the Java code still lives under `feature/companion/feedback/` (§4a) — and, since the ArchUnit stereotype rules bind on the LEAF package, in the usual layer subpackages beneath it (`feedback/{entity,repository,service,controller,mapper}/`), not flat in `feedback/`.
- **HS256 must be set explicitly** in `AuthService` (NimbusJwtEncoder can't infer the alg from a symmetric secret).
- **Multi-user account model since `mezo-qw37` S1** — invite-gated registration, persisted FE token (`localStorage`), per-request `CurrentUser` status check (a JWT stays valid 30 days; disabling an account is enforced at request time, not by revoking the token). See [`_platform-auth-security.md`](_platform-auth-security.md).
- **Push notifications add a third outbound HTTP client, a new client-vs-server error split, and the app's first per-minute cron** (§4g) — `techcore/webpush.WebPushClient` follows the `OffClient`/`WebPageClient` `RestClient` idiom; the three `WEBPUSH_*` codes are split deliberately by fault origin, not by exception type; `NotificationDispatchJob` proves the DB-work/async-send split matters the moment a job runs every minute against a size-1 scheduler pool. Own webpush implementation over a third-party library, per [ADR 0014](../decisions/0014-own-webpush-implementation.md). **N1+N2+N3 all shipped** — see [`_platform-notifications.md`](_platform-notifications.md) for the full design + gotchas.
- **Deferred / not yet adopted:** the remaining Fuel legs + Insights backends and their contract fragments are unbuilt (🔶 mock-only; People shipped in Slice E). **WireMock is IN since Fuel P6** (`wiremock-standalone`, test scope — arrived with the first real external API, the OpenFoodFacts client, exactly as `integration_test_framework.md` prescribed; `PantryImportApiIT` stubs OFF on a dynamic port + `@DynamicPropertySource` base-url override). Mail/Firebase/RabbitMQ mocks and a multi-user role matrix stay deferred. **Phase 3 is live since V0.1–V2.2** (Spring AI 2 + Gemini behind the `CompanionLlm`/`EmbeddingPort` ports, pgvector `memory_embedding` with hibernate-vector `float[]` mapping + native `<=>` ANN queries, the app's first `@Scheduled` cron, and a session-level `hnsw.iterative_scan` GUC via Hikari `connection-init-sql`) — see [`companion.md`](companion.md); RAG recall-in-chat (V2.3) and the pattern engine (V3.x) remain.

---

**Jackson 3 wire-format gotcha (fixed 2026-07-04, V2.2 session):** Jackson's default
`DateTimeFeature.ADJUST_DATES_TO_CONTEXT_TIME_ZONE` silently normalized every incoming
`OffsetDateTime` to UTC — so the FE's deliberately offset-bearing wall-clock timestamps
(`nowOffsetIso` → `takenAt`/`administeredAt`) lost their offset server-side and
`.toLocalDate()` misfiled a 00:00–02:00 local intake/dose to the previous UTC day.
`techcore/configuration/JacksonConfiguration` disables it via a `JsonMapperBuilderCustomizer`
(Jackson 3 moved the flag to `DateTimeFeature`; Boot 4.0.0 has no yml key for it). Test-layer
mirror: `SupplementIntakePopulator` derives `takenDate` in the system zone (FE-faithful), and
the omitted-`takenAt` service fallback stays deliberately UTC (asserted as such).

## 10. Key files

**Contract pipeline**
- `api/base.yml` — info/servers/global bearer security; merge base
- `api/common/common-schemas.yml` — `SystemMessage` + `SystemMessageList`
- `api/generate/merge.yml` — ordered fragment input list (append new fragments here)
- `api/generate/package.json` — `openapi-merge-cli`, `generate:api`
- `api/openapi.yml` — committed merged contract (source of truth)
- `api/feature/{auth,weight,sleep,checkin,train,goal,biometrics-profile,pantry,…}/*.yml` — the **30 per-feature fragments** (of `merge.yml`'s 32 `inputFile` entries — the other two are `base.yml` + `common-schemas.yml`, §4a); the newest is `companion-feedback/companion-feedback.yml`, tag `CompanionFeedback`, `mezo-b3pp.15` — a fragment of its own rather than an addition to `companion.yml`, §4a/§9)

**Backend generator config**
- `backend/pom.xml` (~175-215) — `openapi-generator-maven-plugin` (spring), configOptions, Lombok DTO annotations

**techcore (the reusable spine)**
- `techcore/persistence/OwnedEntity.java`, `OwnedRepository.java` — owned-entity superclass + `findAllOwned`
- `techcore/security/SecurityConfig.java`, `CurrentUserId.java`, `CorsProperties.java` — stateless JWT + ownership resolution + CORS
- `techcore/exception/GlobalExceptionHandler.java`, `SystemMessage.java`, `SystemRuntimeErrorException.java`, `Level.java`, `Type.java` — error contract
- `backend/src/main/resources/messages.properties` — message codes (Hungarian; newest: **`FEEDBACK_REASON_REQUIRES_DOWN`**, `mezo-b3pp.15` — the service-level 400 standing in front of `ck_message_feedback_reason`, §9); `application.yml` — `mezo:` config + `ddl-auto: validate`

**Reference feature (weight — smallest full slice)**
- `feature/biometrics/weight/controller/WeightLogController.java`, `service/WeightLogService.java`, `mapper/WeightLogMapper.java`, `repository/WeightLogRepository.java`, `entity/WeightLogEntity.java`

**Goal + biometric profile (G1, `mezo-2hp` — date-less owned aggregates)**
- `feature/goal/{controller/GoalController,service/GoalService,mapper/GoalMapper,repository/GoalRepository,entity/GoalEntity}.java` + `feature/goal/GoalSeedData.java` (demodata active goal + G3 plan links)
- **G3 plan-link aggregate (`mezo-3sc`):** `feature/goal/entity/GoalPlanLinkEntity.java`, `repository/GoalPlanLinkRepository.java`, `service/{GoalPlanLinkService,GoalTimelineService}.java`, `mapper/GoalPlanLinkMapper.java`; cascade in `service/GoalService.java#deleteGoal`; read-only train finder `feature/train/repository/MesocycleRepository.java#findByIdAndCreatedByAndDeletedFalse`
- `feature/biometrics/profile/{controller/BiometricProfileController,service/BiometricProfileService,mapper/BiometricProfileMapper,repository/BiometricProfileRepository,entity/BiometricProfileEntity}.java`

**Fuel — Pantry (Kamra) (slice C, `mezo-9xu` — single `pantry_item` table, kind-projected)**
- `feature/pantry/{controller/PantryController,service/PantryService,mapper/PantryMapper,repository/PantryItemRepository,entity/PantryItemEntity}.java` + typed-jsonb micros `entity/MicroFact.java`. `PantryService.getPantry` projects by `kind` into `{ ingredients, stash }`; `validatePerKind` holds the per-kind required-field rules (no DB CHECKs). No demodata seed (clean slate).
- contract fragment `api/feature/pantry/pantry.yml` (tag `Pantry` → `PantryApi`; schemas `PantryResponse`/`PantryItemRequest`/`PantryItemResponse`/`IngredientResponse`/`SupplementStashResponse`/`PantryMacros`/`PantryMicro`/`PantryStock`)

**Progression (P1 foundation + P2 gym engine + P3a run engine + P3b sport engine + P4 profile API, `mezo-8e4` — `levelUp` on the Train contract + the profile fragment; see §4e)**
- `feature/progression/{ProgressionCurve,PerkCatalog,ProgressionGate,ProgressionTaxonomy}.java` + `config/ProgressionProperties.java` (`mezo.progression.{curve,gym,run,sport,robustness,radar}`); content `src/main/resources/content/progression-perks.json`
- entities `feature/progression/entity/{SkillProgressEntity,LevelUpEventEntity,PerkUnlockEntity}.java` + typed-jsonb payload `entity/LevelUpResult.java`; repos `repository/{SkillProgressRepository,LevelUpEventRepository,PerkUnlockRepository}.java`
- engine `feature/progression/service/ProgressionService.java + the `RobustnessSource` port (impl: `feature/train/signal/TrainingStreakCalculator`)` (`applyGym`/`applyRun`/`applySport` over the shared private `award(...)` tail; `getProfile` read aggregation); signal records `gym/GymSignal` · `run/RunSignal` · `sport/SportSignal` (the train→progression contract); the calculators live train-side in `feature/train/signal/{GymSignalCalculator,RunSignalCalculator,SportSignalCalculator,TrainingStreakCalculator}.java`; DTO map `mapper/LevelUpResultMapper.java`
- triggers (switch-gated) on the Train side: `WorkoutService.finishWorkout` (gym → `WorkoutInstanceResponse.levelUp`) + `RunningService.logSession` (run → `RunSessionLogResponse.levelUp`) + `SportService.logSportSession` (sport → `SportSessionResponse.levelUp`)
- read surface (P4): `controller/ProgressionController.java` (`@ConditionalOnProperty`-gated `implements ProgressionApi`) + the first `api/feature/progression/progression.yml` fragment (`GET /api/progression/profile`); **FE wiring (cards + dual-mode mock) is P6**

**Auth**
- `feature/auth/...` (`AuthController`, `AuthService`, `OwnerProperties`, `OwnerSeedData`, `entity/AppUserEntity`, `UserProfileEntity`)

**Push notifications (N1+N2+N3 all shipped, `mezo-h4wp.6.1`/`.6.2`/`.6.3` — see §4g; full breakdown in [`_platform-notifications.md`](_platform-notifications.md), not restated here)**
- `techcore/webpush/{WebPushProperties,VapidSigner,Aes128GcmEncryptor,WebPushClient,WebPushResult,WebPushSubscriptionKeys}.java`
- `feature/notification/service/{PushSubscriptionService,PushSender,NotificationPrefService,NotificationScheduleService,DueEvaluator,AnchorResolver,NotificationDispatchJob,PushDispatchExecutor}.java` + `domain/{NotificationCategory,AnchorSet,CategoryPref,DueItem,ScheduleEntry}.java` + `entity/{PushSubscriptionEntity,NotificationPrefEntity,PushLogEntity,NotificationScheduleEntity}.java` + `controller/NotificationController.java`
- `api/feature/notification/notification.yml`; migrations `db/changelog/1.0.0/script/{202607291000_mezo-h4wp.6.1_create_push_subscription,202607291400_mezo-h4wp.6.2_create_notification_pref_and_push_log,202607291500_mezo-h4wp.6.3_create_notification_schedule}.sql`

**Train (largest consumer — one `TrainApi` → six services)**
- `feature/train/controller/TrainController.java`; services `TrainService/WorkoutService/SportService/GymScheduleService/ExerciseCatalogService/ExerciseRecordService/RunningService`; mappers `TrainMapper/RunningMapper`
- typed-jsonb: `entity/ProvenanceEnvelope.java`, `VolumeRecomputeJson.java`, `RunningBlockStructure.java`; discriminator: `entity/WorkoutSessionEntity.java`
- content/seed: `ExerciseCatalogLoader.java` (master content, all profiles), `TrainSeedData.java`, `RunningSeedData.java` (demodata)

**Liquibase**
- `db/changelog/db.changelog-master.yaml`, `1.0.0/1.0.0_master.yml`, `1.0.0/script/*.sql` (bd-ids: v67/n5q/tod/0ae/7ot/b4n/auk + `202606181200_mezo-2hp_create_goal.sql` for `goal` + `biometric_profile` + `202606181600_mezo-3sc_create_goal_plan_link.sql` for `goal_plan_link` + `202606221200_mezo-9xu_create_pantry_item.sql` for `pantry_item` + `202606251200_mezo-8e4_create_progression.sql` for `skill_progress` + `level_up_event` + `perk_unlock`)

**Test framework**
- `support/AbstractIntegrationTest.java` (imports the populators, incl. `PantryItemPopulator`, `SkillProgressPopulator`, `LevelUpEventPopulator`), `ApiIntegrationTest.java`, `ResetDatabase.java` (TRUNCATE list now incl. `goal, biometric_profile, pantry_item, skill_progress, level_up_event, perk_unlock`), `DatabasePopulator.java`, `populator/{UserPopulator,TrainPopulator,RunningPopulator,GoalPopulator,BiometricProfilePopulator,PantryItemPopulator,SkillProgressPopulator,LevelUpEventPopulator}.java`
- canonical ITs: `feature/biometrics/BiometricsContractIT.java`, `feature/train/ProvenanceRoundTripIT.java`; G1: `feature/goal/{GoalServiceIT,GoalContractIT}.java`, `feature/biometrics/profile/{BiometricProfileServiceIT,BiometricProfileContractIT}.java`; G3: `feature/goal/{GoalPlanLinkServiceIT,GoalTimelineServiceIT,GoalTimelineContractIT}.java`; Pantry (`mezo-9xu`): `feature/pantry/{PantryItemRepositoryIT,PantryServiceIT,PantryApiIT}.java`; Progression P1+P2+P3a+P3b (`mezo-8e4`): `feature/progression/{ProgressionCurveTest,PerkCatalogIT,ProgressionPersistenceIT,ProgressionPopulatorIT,ProgressionPropertiesIT}.java` + engine `service/{ProgressionServiceIT,ProgressionRunIT,ProgressionSportIT,ProgressionProfileServiceIT}.java` + signal `run/RunSignalCalculatorIT.java` + `sport/SportSignalCalculatorIT.java` + read surface `controller/{ProgressionProfileApiIT,ProgressionProfileSwitchOffApiIT}.java` + Train-side triggers `feature/train/{WorkoutFinishLevelUpApiIT,RunSessionLevelUpApiIT}.java`

**Frontend seam**
- `frontend/src/data/_client/api.ts` (`apiFetch`, `ApiError`, `setToken`, `API_BASE`), `data/_client/mode.ts` (`isMockMode`), `data/_client/tokenStore.ts` (persisted bearer token), `data/auth/authApi.ts` (login/register/me/changePassword/completeOnboarding), `data/_client/api.gen.ts` (generated), `data/me/biometricsApi.ts`, `data/train/trainApi.ts`, `data/train/runningApi.ts`, `data/me/goalApi.ts`, `data/me/goalLinkApi.ts` (G3 timeline/attach/detach), `data/me/biometricProfileApi.ts`, `data/fuel/pantryApi.ts` (slice C — `/api/pantry` CRUD)
- `frontend/src/data/hooks.ts` (single FE↔data boundary), `data/train/trainHooks.ts`, `data/train/runningHooks.ts`, `data/me/weightHooks.ts`, `data/me/goalHooks.ts` (dual-mode; G3 `useGoal` populates real linked plans from the timeline; G4a adds `useGoalCreation` — the first goal write; G4b adds `useGoalActions` — archive/delete/attach/detach — and widens `useGoal` with raw `goalResponse`/`timeline`/`goalId`; **G6 adds `useFeasibilityPreview`** — debounced live preview, and drops the profile-upsert step from `useGoalCreation`), `data/me/biometricHooks.ts` (**G6 — `useBiometricProfile` empty(all-null)/old-404→null + `useBiometricActions` upsert→invalidate `['biometricProfile']`+`['goals']`**), `data/fuel/pantryHooks.ts` (**slice C `mezo-9xu` — `usePantry` dual-mode read kind-split + `usePantryActions` add/update/delete; re-exported from `hooks.ts`**) consuming `data/fuel/pantryApi.ts`
- mock-only (no backend yet): `frontend/src/data/fuel/fuel.ts`, `pantry.ts` (the static Pantry/recipe seed — the backed inventory CRUD flows through `data/fuel/pantryApi.ts`, not this file), `fuelWeek.ts`, `insights.ts` (`people.ts` is mock-mode-only seed since Slice E — real People traffic flows through `data/me/peopleApi.ts`)

**House-standard references (linked, not restated)**
- `docs/references/api_contract_conventions.md`, `java_package_structure.md`, `spring_patterns.md`, `error_handling.md`, `liquibase_conventions.md`, `testing_standards.md`, `integration_test_framework.md`, `configuration_conventions.md`

**Cross-link to existing docs**
- Phase-2 design: `docs/superpowers/specs/2026-06-10-phase2-backend-design.md`; Train write/clean-slate: `2026-06-11-train-write-clean-slate-design.md`; running slice: `2026-06-14-train-running-slice-design.md`; goal-system G1: `2026-06-18-goal-system-design.md`; push notifications: `2026-07-29-push-notifications-design.md` + [`_platform-notifications.md`](_platform-notifications.md) (living doc); plans under `docs/superpowers/plans/`
- ADR: `docs/decisions/0001-deploy-on-k3s-argocd-learning-track.md`; push notifications: `docs/decisions/0014-own-webpush-implementation.md`; roadmap: `docs/milestones/roadmap.md`; infra: `docs/infrastructure/`

