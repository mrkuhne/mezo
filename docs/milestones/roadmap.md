# mezo — Roadmap & Milestone Log

High-level "where are we" timeline. Update this whenever a phase/slice ships or the direction shifts.
Detail lives elsewhere (`decisions/`, `infrastructure/`, `superpowers/`); this is the index over time.

## Phases

- **Phase 1 — Frontend (mock data):** ✅ done. React 19 + Vite + Tailwind v4, Hungarian UI, 6 vertical slices on a mock data layer.
- **Phase 2 — Core data backend:** 🔄 in progress. Java / Spring Boot 4 + PostgreSQL behind the same `src/data/hooks.ts` boundary.
  - Slice A (foundation + auth + biometrics + TanStack Query): ✅ done
  - Slice B (Train): ✅ done
  - Slice C (Fuel): 🟢 core shipped — Pantry (mezo-9xu), Recipes (mezo-lns), Meal-logging (mezo-arb), **Medication/Gyógyszer (mezo-d94)** closed/backend-backed. Remaining work is a **phased completion plan** ([`fuel-completion-roadmap`](../superpowers/plans/2026-06-26-fuel-completion-roadmap.md), epic `mezo-6r1`): P0 decisions → ✅ P1 **water logging** (shipped mezo-0z5) → ✅ P2 **Stack/Protocol** (shipped mezo-09g) → ✅ P3 **Gyógyszer tab + `medication` domain (first-class) + derived retaDay broadcast** (shipped mezo-d94) → P4 weekly-plan → ✅ P5 **timeline / Mai day-planner** (shipped mezo-9ys) → P6 import → P7 meal-scoring → P8 Phase-3 AI. Stock tracking parked (mezo-6nu).
  - Slice D (Insights seed) → E (People): ⏳ remaining
- **Phase 3 — AI brain:** 🔄 in progress. Context-aware companion chat + layered AI memory (snapshot → tools → facts → RAG → patterns) in 14 session-sized slices (v0 „lát engem" → v1 „megjegyez" → v2 „emlékszik" → v3 „észrevesz"). Epic `mezo-fnnq` (one child per slice); design spec [`phase3-companion-chat-design`](../superpowers/specs/2026-07-03-phase3-companion-chat-design.md) + roadmap [`companion-roadmap`](../superpowers/plans/2026-07-03-companion-roadmap.md) — the spec distills the old Supabase-era PRD/architecture (`docs/old docs/`) onto Spring Boot 4 + Spring AI + pgvector. ✅ V0.1 (`mezo-fnnq.1`): [ADR 0008](../decisions/0008-companion-llm-spring-ai-2-gemini.md) — Spring AI 2.0 + Gemini + `CompanionLlm` port skeleton behind `mezo.feature.companion.enabled`. Next: V0.2 (`mezo-fnnq.2`, conversations + sync chat endpoint) or V3.1 (`mezo-fnnq.12`, unblocked in parallel).

## Cross-cutting tracks

- **Deployment / infra (learning track):** 🔄 planned — self-managed k3s + ArgoCD + pgAdmin on a single Hetzner VPS.
  Driver `mezo-ht3`. See [ADR 0001](../decisions/0001-deploy-on-k3s-argocd-learning-track.md) and
  [deployment architecture](../infrastructure/deployment-k3s-argocd.md). Goal doubles as hands-on practice
  with the stack Daniel's clients use.

## Milestone log

| Date | Milestone |
|---|---|
| 2026-07-03 | **Companion V0.2 — Conversations + sync chat endpoint (`mezo-fnnq.2`)** shipped: the Phase-3 chat **persistence spine**. Two owned tables (`ai_conversation` + `ai_message`, UUID/`created_by`/soft-delete, `tool_calls`/`refs` typed jsonb envelopes left null until V0.5), a contract fragment `api/feature/companion/companion.yml` (`GET/POST /api/companion/conversation`, `GET .../messages`, `POST .../message` — **sync JSON** answer, SSE deferred to V0.4), and two switch-gated services: `ConversationService` (CRUD spine) + `ChatService` (static HU companion-voice `SYSTEM_PROMPT` + last-20-message history windowing rendered into the prompt → one `CompanionLlm.complete()` → persists both turns; auto-title = first user message truncated to 80). Config `mezo.companion.chat.{history-window:20, title-max-chars:80}`. Every companion bean stays behind `mezo.feature.companion.enabled` (switch off ⇒ the whole `/api/companion/*` surface 404s). 5 new ITs (jsonb round-trip, conversation/chat services, HTTP flow, switch-off) against the `companion-fake` echo LLM — network never touched. FE untouched (mock ChatPage stays until V0.4). Living doc [`docs/features/companion.md`](../features/companion.md) born. |
| 2026-07-03 | **Companion V0.1 — Spring AI + provider ADR + skeleton (`mezo-fnnq.1`)** shipped: [ADR 0008](../decisions/0008-companion-llm-spring-ai-2-gemini.md) picks **Spring AI 2.0.0** (the Boot-4 line) + **Google Gemini** (`spring-ai-starter-model-google-genai`; tiers `gemini-2.5-flash`/`-pro` as config) + `GEMINI_API_KEY` env/SealedSecret delivery. `feature/companion` package born: `CompanionLlm` port + `GeminiCompanionLlm` real adapter + `FakeCompanionLlm` (`companion-fake` profile, tests never touch the network), all behind `mezo.feature.companion.enabled`; `CompanionProperties` (`mezo.companion.llm.*`). Proofs: 454 ITs green **without** an API key (dummy-default boot), real Gemini streamed hello via the `companion-smoke` profile runner. Unblocks V0.2 (chat spine) and V3.1 (patterns). |
| 2026-07-03 | **Phase 3 (AI brain) mapped**: the old Supabase-era PRD+architecture (`docs/old docs/`) surveyed and distilled into a current-stack design spec (`2026-07-03-phase3-companion-chat-design.md`) + a 14-slice companion roadmap (`2026-07-03-companion-roadmap.md`, epic `mezo-fnnq`, session-per-slice handoff). Key calls: deterministic context snapshot (not RAG) is the chat's backbone; RAG = episodic layer over narrative units; facts via confirm-promote pipeline; old Phase-7 motivation system explicitly out (XP-vs-narrative tension parked for a future ADR). Fuel P8 umbrella (`mezo-0h6w`) note-linked. |
| 2026-06-13 | Deploy direction decided: self-managed k3s + ArgoCD + pgAdmin (learning track). ADR 0001 + infra doc written. `docs/` decision/infra/milestone structure established. |
| 2026-06-14 | Deploy steps 0-2 done: Hetzner VPS hardened + Tailscale, k3s installed, full mezo (Postgres + backend + frontend) live on k8s at `https://46.225.112.172.sslip.io/` with Let's Encrypt HTTPS. `k8s/` manifests + Dockerfiles committed. Pending: pgAdmin (step 3), ArgoCD/GitOps (step 4). |
| 2026-06-24 | Slice C (Fuel) core shipped: Pantry (`pantry_item`, mezo-9xu) + 146-item catalog (mezo-zza), Recipes (`recipe`/`recipe_ingredient`, mezo-lns), Meal-logging (`meal`/`meal_item` + `/api/fuel/day/{date}`, mezo-arb) — backends + dual-mode FE, green both modes. Remaining Fuel (Stack/Protocol, weekly-plan, meal-scoring) Phase-3-deferred. |
| 2026-06-25 | Progression P1 (domain foundation) — curve, 3 tables (`skill_progress`/`level_up_event`/`perk_unlock`), entities/repos, perk catalog; triggers/HTTP deferred to P2 (`mezo-8e4`). |
| 2026-06-26 | Fuel polish (mezo-ksh/dh6/8xy/cki) + stock UI parked behind a flag (mezo-6nu). **Fuel completion roadmap** authored (`fuel-completion-roadmap.md`, epic `mezo-6r1`, phases P0–P8, dependency-ordered + deterministic-first); duplicate epic mezo-4ag closed. |
| 2026-06-26 | Fuel **P3 — Medication / Gyógyszer (mezo-d94)** shipped: `medication` + `medication_dose` tables as a **first-class** domain (NOT a pantry row), `/api/medication` + `…/{id}/dose` endpoints, server-derived 7-day Reta cycle (`MedicationCycleService`, retaDay from the dose log), the `/fuel/gyogyszer` tab + `LogDoseSheet`, and the **derived `retaDay` broadcast** via `useTodayScenario()` (every Reta surface reads it). Backend + dual-mode FE, green both modes. Supersedes the spec's earlier "Reta in pantry" P0b idea. |
| 2026-06-27 | Progression **P3b — SPORT path** (`mezo-lmox`, child of `mezo-8e4`) shipped: `sport_session` generalized into a 3-kind modality (the existing `sport` column became the `volleyball\|cross\|trx` discriminator via `ck_sport_session_sport`, + a `rounds` effort column), `ProgressionService.applySport` granting per-kind athletic XP, and a switch-gated `levelUp` on `SportSessionResponse` (mirrors gym finish + run log). Two baton refactors rode along: the streak **robustness rate lifted out of `gym()` into a shared `mezo.progression.robustness` node**, and `RunSignalCalculator` hardened to an ownership-scoped log load. Backend + dual-mode FE green (657/657 FE both modes). FE cross/TRX log-sheet + `LevelUpScreen` deferred to P5. Also fixed a time-of-day-flaky medication test (`mezo-yc9z`). |
| 2026-06-29 | Progression **P4 — profile endpoint** (`mezo-x71h`, child of `mezo-8e4`) shipped: `GET /api/progression/profile` — the **first** `api/feature/progression/` contract fragment and the **first** switch-gated controller (`ProgressionController` `@ConditionalOnProperty(PROGRESSION_SWITCH)`). `ProgressionService.getProfile` aggregates `skill_progress` over the fixed taxonomy: athlete-level (mean of the 11 non-robustness athletic, missing→level 1, `null`=ghost), 6 fixed radar axes (Erő blends the muscle-level mean via `mezo.progression.radar.strength-muscle-blend`), streak, and best-athletic/best-muscle highlights. Backend green (409 tests); FE green (regen only — cards + dual-mode mock are **P6**). |
| 2026-06-29 | Progression **P5 — FE level-up overlay** (`mezo-te8k`, child of `mezo-8e4`) shipped — **frontend-only**: the `levelUp` already on all three finish responses is now **captured** (gym `finishWorkout` + sport/run log mutations forward the response) and presented via a **single host** (`LevelUpProvider`/`useLevelUp`, mounted in `AppLayout`) — a full-bleed `LevelUpScreen` portal overlay over the chrome (hand-rolled CSS keyframes, rAF count-up, **`prefers-reduced-motion` first-class** via the new shared `useReducedMotion`; scales to multiple level-ups; no-level-up case never dead-ends). Gym layers it over the `WorkoutComplete` recap (revealed on `Tovább`) and the recap's PR framing now uses the real `max_strength` level-up signal. Also shipped the **cross/TRX kind selector** in `SportLogSheet` (de-hardcoded volleyball → `sport: volleyball\|cross\|trx` + `rounds`) and the deferred-close sheet contract (`onSave(input, done)`). Mock mode returns seeded `LevelUpResult` fixtures. FE green both modes (685/685) + build; visual verified. Profile cards (radar/muscle) remain **P6**. |
| 2026-07-02 | Fuel **P1 — Water logging (mezo-0z5)** shipped: a `water_log` table + `POST/DELETE /api/water-log` (on `MealController`), and `FuelDayService.consumed.water` is now a real per-day Σ (was always `= targets.water`, a fake 100% ring). FE: dual-mode `useWaterActions.logWater` + `+250/+500 ml` chips on `MacroHero`. Goal/Reta-aware water targets stay deferred (config constant). Backend + dual-mode FE green. |
| 2026-07-02 | Fuel **P2 — Stack / Protocol (mezo-09g)** shipped: the **first Fuel-owned backend package** (`backend/.../feature/fuel` + new `api/feature/fuel/fuel.yml`) — `protocol` + `protocol_item` + `supplement_intake` tables, `ProtocolService` (activate = supersede + `v+1`, confidence from `mezo.fuel.protocol.default-confidence` config) + `IntakeService`, `FuelController` (`GET/POST /api/fuel/protocol` + `GET/POST /api/fuel/intake` + `DELETE …/intake/entry/{id}`). FE: dual-mode `useStack`/`useProtocol` + `useStackActions`/`useProtocolActions` (`data/fuel/fuelApi.ts` + `stackHooks.ts`); Stack "Bekapcsolás · ma" is a **real** activation + slot tap-to-log intakes; Mai protocol-meta reads the real protocol (hidden at v0). The protocol persists **selection + version metadata only** (`buildProtocol` slots stay FE-computed); "Mentés protokollként" deferred; `useStackRecommendations` stays `[]` in real mode until P8. Folded in **mezo-4nu #1** (Stack `/api/goals` decouple via `useStackContext`). Backend + dual-mode FE green. |
| 2026-07-03 | Fuel **P5 — Fuel-Timeline / Mai day-planner (mezo-9ys)** shipped: the hand-authored Mai pacing timeline + Today's preview are now a **real client-composed day-planner** — a pure `buildDayPlan` (`features/fuel/logic/buildDayPlan.ts`) merges planned meal/snack windows (spread wake→kitchen-close, snapped around the day's REAL gym/sport/run blocks pre −75m / post +45m) + per-slot budgets from the goal `prescription` current-week segment (`deriveDailyBudget`, config `FuelDay.targets` fallback) + ±20% recipe-fit suggestions (`|Δkcal|→starred→|Δp|`) + the anchor-aware supplement protocol/intakes. Dual-mode `useFuelTimeline`/`useFuelPreview` (mock seed byte-parity / real composition). **Only backend work: three day-planner settings on the goal** (`mealsPerDay`/`wakeTime`/`bedTime` — the **FE's first goal write-back** via `EditGoalSheet` "Napi ritmus" → full-replace `goalResponseToUpsert` PUT). Client-side merge, no new Fuel endpoint. Sleep-derived anchors + rebalancing + prose → P7/P8. Backend + dual-mode FE green. |
| 2026-06-29 | Progression **P6 — FE profile cards** (`mezo-xje5`, child of `mezo-8e4`) shipped — **frontend-only**, and **completes the `mezo-8e4` epic** (P1 domain → P2 gym → P3a run → P3b sport → P4 profile API → P5 level-up overlay → P6 profile cards). A dual-mode `useProgressionProfile()` (`useDualQuery`: seeded `progressionProfileMock` / real `GET /api/progression/profile` / ghost `realEmpty` on switch-off 404) feeds two cards below `BiometricCard` in `ProfileView`: **`AthleticRadarCard`** (hand-rolled SVG hexagon radar of the 6 server-computed `radarAxes` via the pure `radarGeometry` helper + athlete-level/best/streak stats) and **`MuscleLevelsCard`** (top-N by level + reserve note). Both ghost on `athleteLevel == null` (mirrors BiometricCard) and reuse the P5 `useReducedMotion`; "Teljes profil"/"Mind a 13" links are inert v1 stubs. New `progressionApi.ts` GET client (no contract regen — P4 types already generated). FE green both modes + build; browser-verified. |
