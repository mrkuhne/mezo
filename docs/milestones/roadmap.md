# mezo — Roadmap & Milestone Log

High-level "where are we" timeline. Update this whenever a phase/slice ships or the direction shifts.
Detail lives elsewhere (`decisions/`, `infrastructure/`, `superpowers/`); this is the index over time.

## Phases

- **Phase 1 — Frontend (mock data):** ✅ done. React 19 + Vite + Tailwind v4, Hungarian UI, 6 vertical slices on a mock data layer.
- **Phase 2 — Core data backend:** 🔄 in progress. Java / Spring Boot 4 + PostgreSQL behind the same `src/data/hooks.ts` boundary.
  - Slice A (foundation + auth + biometrics + TanStack Query): ✅ done
  - Slice B (Train): ✅ done
  - Slice C (Fuel): 🟢 core shipped — Pantry (mezo-9xu), Recipes (mezo-lns), Meal-logging (mezo-arb), **Medication/Gyógyszer (mezo-d94)** closed/backend-backed. Remaining work is a **phased completion plan** ([`fuel-completion-roadmap`](../superpowers/plans/2026-06-26-fuel-completion-roadmap.md), epic `mezo-6r1`): P0 decisions → ✅ P1 **water logging** (shipped mezo-0z5) → ✅ P2 **Stack/Protocol** (shipped mezo-09g) → ✅ P3 **Gyógyszer tab + `medication` domain (first-class) + derived retaDay broadcast** (shipped mezo-d94) → ✅ P4 **weekly-plan / Terv real + gym-time write-through** (shipped mezo-kpo) → ✅ P5 **timeline / Mai day-planner** (shipped mezo-9ys) → P6 import → P7 meal-scoring → P8 Phase-3 AI. Stock tracking parked (mezo-6nu).
  - Slice D (Insights seed) → E (People): 🔄 **re-mapped 2026-07-04** into the [`phase2-completion-roadmap`](../superpowers/plans/2026-07-04-phase2-completion-roadmap.md) (epic `mezo-t16y`): the original seed-only Slice D is **dropped as superseded** (Phase 3 built the real pattern/fact/chat stack); remaining Phase 2 = Fuel P4/P6/P7 (`mezo-6r1`) + **D′** Insights-Weekly deterministic + honest surface (`mezo-t16y.1`) + **E** People (`mezo-t16y.2`) + **T** Today honest completion (`mezo-t16y.3`) + exit audit (`mezo-t16y.4`).
- **Phase 3 — AI brain:** ✅ **done (2026-07-04)** — all 14 slices shipped in one continuous build-out. Context-aware companion chat + layered AI memory (snapshot → tools → facts → RAG → patterns) in 14 session-sized slices (v0 „lát engem" → v1 „megjegyez" → v2 „emlékszik" → v3 „észrevesz"). Epic `mezo-fnnq` (one child per slice); design spec [`phase3-companion-chat-design`](../superpowers/specs/2026-07-03-phase3-companion-chat-design.md) + roadmap [`companion-roadmap`](../superpowers/plans/2026-07-03-companion-roadmap.md) — the spec distills the old Supabase-era PRD/architecture (`docs/old docs/`) onto Spring Boot 4 + Spring AI + pgvector. ✅ V0.1 (`mezo-fnnq.1`): [ADR 0008](../decisions/0008-companion-llm-spring-ai-2-gemini.md) — Spring AI 2.0 + Gemini + `CompanionLlm` port skeleton behind `mezo.feature.companion.enabled`. ✅ V0.2 (`mezo-fnnq.2`): conversations + sync chat endpoint (persistence spine). ✅ V0.3 (`mezo-fnnq.3`): `ContextSnapshotAssembler` — the chat already knows "today" in the first message. ✅ V0.4 (`mezo-fnnq.4`): SSE streamed turn + the FE ChatPage goes real (dual-mode, honest degraded state; the hand-written-SSE-controller contract precedent). ✅ V0.5 (`mezo-fnnq.5`): tool calling + real tool-chips — **v0 „lát engem" complete**. ✅ V1.1 (`mezo-fnnq.6`): `knowledge_fact`/`learned_fact` tables + fact CRUD + top-N prompt injection. ✅ V1.2 (`mezo-fnnq.7`): post-turn async fact extraction + the real KnowledgeListPage confirm UI (accept/refine/reject). ✅ V1.3 (`mezo-fnnq.8`): never-ask-twice + advisor chain v1 (clinical regex + LLM verdict, retry-once → `degraded` flag, reinforcement on dedupe-hit) — **v1 „megjegyez" complete**. ✅ V2.1 (`mezo-fnnq.9`): pgvector infra (image swap in all 3 envs incl. live k3s, `memory_embedding` vector(768)+HNSW, `EmbeddingPort` real/fake). ✅ V2.2 (`mezo-fnnq.10`): daily summaries + embed pipeline (first `@Scheduled` cron, catch-up=backfill, post-turn turn embedding). ✅ V2.3 (`mezo-fnnq.11`): `find_similar_past_days` recall tool (similarity × recency-decay, similarity floor) — **v2 „emlékszik" complete**. ✅ V3.1 (`mezo-fnnq.12`): nightly Pearson pattern engine + Inbox — **PatternsPage goes real**. ✅ V3.2 (`mezo-fnnq.13`): weekly smart-tier hypothesis loop (propose→critique→revise, §4.7 scoring). ✅ V3.3 (`mezo-fnnq.14`): pattern→knowledge promotion + recurrence reinforcement + in-chat acknowledgment — **the epic is COMPLETE: all 14 slices of v0 „lát engem" → v1 „megjegyez" → v2 „emlékszik" → v3 „észrevesz" shipped.** **LIVE in production since 2026-07-04 reggel** (GEMINI_API_KEY sealed, switch on, real-answer smoke passed). Next epics: proactive layer (briefing/heartbeat/memoir) and Fuel P8, both on top of this stack.

## Cross-cutting tracks

- **Deployment / infra (learning track):** 🔄 planned — self-managed k3s + ArgoCD + pgAdmin on a single Hetzner VPS.
  Driver `mezo-ht3`. See [ADR 0001](../decisions/0001-deploy-on-k3s-argocd-learning-track.md) and
  [deployment architecture](../infrastructure/deployment-k3s-argocd.md). Goal doubles as hands-on practice
  with the stack Daniel's clients use.

## Milestone log

| Date | Milestone |
|---|---|
| 2026-07-04 | **T — Today honest completion (`mezo-t16y.3`)** shipped — the landing tab stops lying in real mode, **zero new backend** (the check-in GET already existed). `useToday` → dual-mode composition over `useTrain()` + the real date: header (HU weekday + `huMonthDay`, meso week/phase/title chips, honest hide when absent), workout teaser = Train's today session (hidden on rest days; the fabricated "Prediction · 0.72" row and the "Stacked day" note became mock-only data props), volleyball card = real sport schedule (real `today:true`). New `useQuickStats` (sleep+weight derived, **HRV cell dropped** — no source) + `useInsightsTeaser` (top proposed pattern from the real V3.1 inbox, `/insights` deep-link, hidden when none/degraded). **Check-in strip finally READS** (`GET ?date=` overlaid on the 4 canonical slots; empty slots derive from wall-clock: past→skipped/now/pending; save invalidates the day query — reload keeps the badge, live-verified against the demodata backend). Briefing prose stays static behind an honest **„Demo tartalom"** label (generated briefing = proactive epic); `?day=/?retaDay=` demo params survive by design. Gates: FE 907/907 both modes + build; parity byte-safe (mock statics unchanged). Spec [`2026-07-04-today-honest-completion-design`](../superpowers/specs/2026-07-04-today-honest-completion-design.md). Filed `mezo-edrv` (pre-existing `['weightLog']` queryFn-less console noise). |
| 2026-07-04 | Fuel **P4 — Weekly Plan / Rhythm · Terv real (mezo-kpo)** shipped — the first slice of the Phase-2 completion roadmap. `useFuelWeek` → composing dual-mode `data/fuel/fuelWeekHooks.ts`: real mode renders Train's derived gym week + sport-schedule volleyball, the Reta strip from the medication cycle (`toRetaCells`; card hides on ghost cycle), and weekly stats (kcal avg / protein-hit days) from the **new `GET /api/fuel/week/{start}`** 7-day rollup (`FuelDayService.getWeek` — the server aggregate D′ will reuse); title date-derives (`"Jún 29 – Júl 5"`), the demo-prose surfaces (patterns / supplement map / coach note / adherence %) go honest-empty (`[]`/`—`). **Gym-time edits persist**: `GymScheduleSheet` saves write through to Train's `PUT /api/train/gym-schedule` (`useFuelWeekActions().saveGymSchedule`, P0a: Fuel = secondary editor; day+time only, type/active stay meso-owned). No new tables. Gates: BE 683 green, FE 899/899 both modes + build. Spec [`2026-07-04-fuel-p4-weekly-plan-design`](../superpowers/specs/2026-07-04-fuel-p4-weekly-plan-design.md). |
| 2026-07-04 | **Phase 2 completion mapped** (companion-pattern spec+roadmap replication): full hook-level mock/real audit → [`phase2-completion-roadmap`](../superpowers/plans/2026-07-04-phase2-completion-roadmap.md) + epic `mezo-t16y`. Key decision: **old Slice D (Insights seed-only) dropped as superseded** — Phase 3 shipped the real pattern/fact/chat stack, so what remains is D′ (deterministic Weekly review + mezo-lfw honest-surface pass over Memoir/Predictions/Experiments), E (People: `person`+`mention` + `usePeople` swap + `useProfile` decision), T (Today honest completion — the landing tab is the last mostly-fake surface: workout/volleyball/quick-stats/check-in-read/InsightsTeaser go real by composition), X (exit audit), sequenced with Fuel P4/P6/P7 (`mezo-6r1`). Everything LLM-shaped (briefing/memoir/suggestion prose, predictions, replan, stack recs) stays ⛔ for the proactive epic / Fuel P8. Each slice = fresh-session continuable (handoff contract in the roadmap header). |
| 2026-07-04 | **Companion V3.3 — pattern→knowledge promotion + reinforcement (`mezo-fnnq.14`)** shipped: **the learning loop closes — and with it the WHOLE Phase-3 companion epic (14/14 slices, `mezo-fnnq`)**. First confirm on a pattern promotes it into a durable `knowledge_fact` (`source=pattern`, title as factText, `promoted_fact_id` link back; v1 category heuristic physiology/trigger→health, response→train; repeat confirms never duplicate; un-confirm leaves the fact — the Knowledge tab owns it). Nightly re-detection of a CONFIRMED pattern in the SAME r-direction reinforces the promoted fact (`reinforcement_count++`, raising its top-N injection rank); direction flips and monitoring rows never reinforce. Freshly promoted facts (≤ `facts.pattern-ack-days` 3) ride an `ÚJ FELISMERÉSEK` block into both chat paths — the companion says „ezt megtanultam rólad". Knowledge tab: additive `KnowledgeFactResponse.patternTitle` → `minta:` evidence chip. Tests: promotion/no-dupe/evidence-link API cases, reinforcement same-sign/flip/monitoring detection cases, ack-block prompt-order ChatServiceIT case, FE chip both modes. Gates: BE 677 green, FE 887/887 both modes. **A companion mostantól: látja a napod, megjegyzi amit megerősítesz, felidézi a hasonló napokat, észreveszi a mintáidat — és amit te megerősítesz, azt tartósan megtanulja.** |
| 2026-07-04 | **Companion V3.2 — AI hypothesis loop (`mezo-fnnq.13`)** shipped: the engine goes beyond pairwise stats — **mechanism-level hypotheses that survive self-critique**. Weekly smart-tier cron (`HypothesisJob`, Sun 03:00, `mezo.techcore.cron.hypothesis-job.enabled`): `HypothesisPipelineService` stages, each pure-compute or pure-LLM (NFR-M-4) — gather (last-7 summaries + confirmed facts + the V3.1 statistical patterns' r/n/p as GROUNDED statistical support) → propose (strict-JSON, **`gemini-2.5-pro` debuts** via the port's new `completeSmart`; the fake stays single-dispatch) → 4-factor critique + prose reasoning → score (0.35/0.25/0.20/0.20 — code constants, arch §4.7) → keep ≥0.75 / revise-once ≥0.50 / discard; defensive JSON at every stage. Survivors = `kind=ai_hypothesis` Inbox rows: confidence=score, critique jsonb (+`reasoning` → additive `PatternResponse.thinking`, no migration), identity `hyp-`+title-hash, ANY-status dedupe (rejected never re-proposed). FE: zero component work (PatternCard already renders confidence/critique/thinking); `toPattern` maps `thinking`. Tests: 9 new IT cases (keep/discard/revise-e2e via `[fake-hypotheses/critique/revise:…]` sentinels — critique/revise matchers scoped to the hypothesis, never the shared context; dedupe, broken-JSON, cap, no-context, switch-off, bindings). Gates: BE 670 green, FE 886/886 both modes. Plan: [`companion-v32-hypothesis-loop`](../superpowers/plans/2026-07-04-companion-v32-hypothesis-loop.md). Next: V3.3 promotion — the epic closes. |
| 2026-07-04 | **Companion V3.1 — statistical patterns + Inbox (`mezo-fnnq.12`)** shipped: **v3 „észrevesz" started — the system surfaces real correlations and Daniel judges them.** The second nightly cron (`PatternDetectionJob`, 02:40, `mezo.techcore.cron.pattern-detection-job.enabled` — the config-reference example key, now real): 8-pair config catalog (`mezo.companion.patterns.pairs`: sleep↔next-day RPE ×2, late-meal↔sleep, Reta-day↔kcal, sport-load↔gym-volume, kcal↔weight-delta, stress↔sleep, water↔energy) → `MetricSeriesService` (12 `MetricKey` extractors over EXISTING owning-feature reads, zero new cross-feature finders; deterministic aggregation, absence never bridged) → lag-align + `min-n` 8 gate → **pure `PearsonCorrelation`** (r + n + real two-sided p via incomplete-beta t-test; scipy-pinned fixtures) → upsert per `(user, kind, pair_key)` (partial-unique; `proposed`/`monitoring` refresh, user-judged rows frozen). Honest numbers: `confidence=NULL` statistical rows („tanulom"), deterministic HU mechanism + `r=/n=/p=/window` evidence chips. Contract: `GET /api/companion/pattern` + `POST …/{id}/decision` (confirm/monitor/reject — REPEATABLE, a pattern is a standing judgement). FE: **PatternsPage real dual-mode** (`patternsApi`/`patternsHooks`, knowledge recipe), PatternCard decisions persist (badge from the PERSISTED status), critique bars conditional (V3.2), degraded 404 card; `recentlyConfirmed` derived from confirmed rows. Tests: 21 new BE (math fixtures, series aggregation/gaps, detect idempotence/freeze/n-gate, API contract incl. repeatable decisions, switch-off, bindings) + FE both modes 886/886. Gates: BE 660 green, FE build+dual green. Plan: [`companion-v31-statistical-patterns`](../superpowers/plans/2026-07-04-companion-v31-statistical-patterns.md). Next: V3.2 hypothesis loop. |
| 2026-07-04 | **Companion V2.3 — similar-days recall (`mezo-fnnq.11`)** shipped: **v2 „emlékszik" is complete** — "volt már ilyen napod?" gets a real answer. `find_similar_past_days(description, k)` joins the audited tool registry (9th tool): `MemoryRecallService` embeds the query (`EmbeddingPort.embedQuery`), ANN-searches the **daily-summary** vectors (kind-scoped — days come from summaries; chat-turn vectors reserved for a later always-on layer) and re-ranks in code by **similarity × exp(-age/τ)** (`mezo.companion.recall.*`: decay-days 90, max-k 5, min-similarity 0.25 floor — weak cosine = noise, honest `nincs adat`; candidate-pool 20). Recalled days ride the chips as `Memory`/date refs — zero FE change. LLM/provider-free tests via the `[fake-embed:…]` scripted query + hand-seeded axis vectors: ranking order, decay math (e^(-200/90) asserted), floor, kind scope, k clamp, render + audit refs (8 new IT cases). Gates: BE 638 green. Next: V3.1 statistical patterns + Inbox (`mezo-fnnq.12`). |
| 2026-07-03 | **Companion V2.2 — daily summaries + embedding pipeline (`mezo-fnnq.10`)** shipped: **the narrative memory fills itself**. `daily_summary` table (partial-unique per user+day `where is_deleted=false` — soft-delete ⇒ regenerable) + `DailySummaryService`: deterministic date-scoped HU digest of one finished day (train sets/sport/run, FuelDay kcal/P + meal titles, sleep, weight, Reta ciklusnap + dose, check-ins; owning-feature reads, one new plain `WeightLogRepository` by-date finder) → ONE cheap-tier port call (`SUMMARY_MARKER` prompt) → past-tense narrative; empty day ⇒ no row, existing ⇒ returned LLM-free. **The app's first `@Scheduled` cron**: `DailySummaryJob` (02:20, `mezo.companion.summary.cron`; switch `mezo.techcore.cron.daily-summary-job.enabled`; `SchedulingConfiguration` born) — per user × finished day in `catch-up-days`(7): generate + embed what's missing, **idempotent catch-up = backfill**, per-date failure isolation, plus a turn-embedding self-heal pass. Embed pipeline: `MemoryEmbeddingWriter` (unit → `EmbeddingPort` → `memory_embedding`; content capped at `embed-max-chars` BEFORE embedding; exists-probe + uq floor), summaries as kind=`daily_summary`, chat turns as kind=`chat_turn` **one vector per turn** (ref = assistant msg id; `ChatTurnCompleted` grew `assistantMessageId`); post-turn `TurnEmbeddingListener` (AFTER_COMMIT async, toggle `embedding.embed-chat-turns`). Fake-LLM `[fake-summary:…]` sentinel + digest echo → 18 new LLM-free IT cases (generation/idempotence/failure-isolation, writer cap/idempotence/catch-up, listener e2e via Awaitility, both switch-offs, bindings). Review round (3-lens + adversarial verify): per-turn-tx catch-up (uq collision can't abort the batch), occurred_on unified onto the message row's day, summary replace-by-day, toggle-heal-not-bypass, redundant index dropped. **Bonus prod bugfix:** Jackson 3's ADJUST_DATES_TO_CONTEXT_TIME_ZONE was silently discarding the FE's UTC offset — a 00–02h local intake/dose misfiled to the previous UTC day; fixed with a JsonMapperBuilderCustomizer (`JacksonConfiguration`) + FE-faithful test layer. Gates: BE 630 green. FE untouched. **Session note:** root-caused the "flaky Lombok compile" — the IDE's Lombok-less JDT compiler races Maven in `target/classes`; local `.classpath` outputs now point to `target/jdt/*`. Next: V2.3 similar-days recall. |
| 2026-07-03 | **Companion V2.1 — pgvector infra + embedding port (`mezo-fnnq.9`)** shipped: **v2 „emlékszik" started** — the vector layer exists everywhere the app runs. Postgres image → `pgvector/pgvector:pg16` in ALL THREE environments (local compose — volume recreated; **live k3s StatefulSet** — pre-swap `pg_dump` to `~/MrKuhne/mezo-live-backups/`, ArgoCD rolled it, collation `REFRESH` + `REINDEX` applied, data verified intact; Testcontainers `asCompatibleSubstituteFor`). Liquibase `202607032033_mezo-fnnq.9`: `CREATE EXTENSION IF NOT EXISTS vector` + `memory_embedding` (uuid/created_by/soft-delete house columns; `kind chat_turn\|daily_summary\|weekly_summary`, `ref_id` with `uq_(kind,ref_id)` as the V2.2 pipeline's idempotence anchor, `content` verbatim, **`embedding vector(768)`**, `occurred_on` recency key; HNSW `vector_cosine_ops` + `(created_by,kind,occurred_on desc)` indexes). Entity maps `float[]` via **hibernate-vector** (Boot-BOM managed, new dep); ANN search = native-SQL `findNearest` (`<=>`, explicit `is_deleted=false` — `@SQLRestriction` doesn't cover native) returning a `MemoryMatch` projection with `distance`. **`EmbeddingPort`** (768 = structural constant): `embedDocuments`/`embedQuery` with Gemini task types; real `GeminiEmbeddingAdapter` hits the GenAI SDK `Client` bean **directly** (Spring AI 2.0.0 ships no Gemini EmbeddingModel — slice decision, port hides it; same `GEMINI_API_KEY`), client-side L2-normalization; `FakeEmbeddingAdapter` (seeded-random unit vectors + `[fake-embed:…]` sentinel). Config `mezo.companion.embedding.model: gemini-embedding-001`. 13 new IT cases (`MemoryEmbeddingRepositoryIT` axis-vector geometry: order/kind/ownership/soft-delete/k/uq + extension probe; `FakeEmbeddingAdapterIT`; wiring + properties asserts); populator + TRUNCATE growth rules followed. Gates: BE 608 green. FE untouched. Next: V2.2 daily summaries + embed pipeline. |
| 2026-07-03 | **Companion V1.3 — never-ask-twice + advisor chain v1 (`mezo-fnnq.8`)** shipped: **v1 „megjegyez" is complete** — the companion stops re-asking known things and self-checks its answers. New `feature/companion/advisor/` package with old-docs §4.5 retry semantics on the port: `ClinicalOutputCheck` (deterministic accent-folded regex — Rx term from `advisors.rx-terms` + dose-change verb in one sentence; graduates the clinical guard from prompt-only) runs first, then `TurnVerdictCheck` — ONE cheap-tier LLM verdict per answer (strict-JSON `{redundantQuestion, ungroundedClaim, reason}` judged against the turn's full prompt + tool-call list, defensively parsed, **fail-open**). Violation → one corrective re-prompt (`AdvisorRetry.block`, same tools + SAME audit) → re-check; still violating ⇒ `ai_message.degraded=true` (additive migration) + `MessageResponse.degraded` on the wire. Sync path retries pre-delivery; the streamed path reviews post-hoc between last delta and `done` (done row authoritative — the FE swap silently carries the corrected answer). FE: subtle `nem ellenőrzött` badge on flagged bubbles. Reinforcement loop starts: an extraction dedupe-hit on a CONFIRMED fact now does `reinforcement_count++` + `last_reinforced_at`. Everything behind `mezo.companion.advisors.enabled` (off ⇒ V1.2 byte-for-byte); stateless fake-verdict sentinels (`[fake-violate…]`) keep ITs LLM-free. Gates: BE 593 green, FE 878/878 both modes. Plan: [`companion-v13-advisors`](../superpowers/plans/2026-07-03-companion-v13-advisors.md). Next: V2.1 pgvector infra. |
| 2026-07-03 | **Companion V1.2 — fact extraction + confirm UI (`mezo-fnnq.7`)** shipped: the learning loop's front half — **the chat now proposes what it learned, Daniel confirms with one tap**. Backend: `ChatTurnCompleted` event (sync + streamed turn) → `FactExtractionListener` (AFTER_COMMIT + `@Async`, gated on `mezo.companion.extraction.enabled`; `AsyncConfiguration` born) → `FactExtractionService` — one cheap-tier LLM call over the turn transcript via the port (strict-JSON, defensively parsed: first-`[`-to-last-`]`, unknown category/blank dropped), normalized string-dedupe vs confirmed facts + pending candidates, per-turn cap (`max-candidates-per-turn: 3`) → undecided `learned_fact` rows (`+category` column, additive migration). Decision surface: `GET /api/companion/fact/candidate` + `POST .../{id}/decision` (`accept\|reject\|refine`; accept/refine promote into `knowledge_fact` source=`chat` → the V1.1 top-N injection carries them; refine-without-text = FIELD error; re-decide = 400 `COMPANION_CANDIDATE_ALREADY_DECIDED`). FE: **KnowledgeListPage goes real dual-mode** — `knowledgeApi` + `useKnowledge`/`useKnowledgeActions` (`['knowledge']` dual-read: facts+candidates, mock-only edges, 404→degraded), L2 candidate cards (Elfogad / Pontosít inline / Elvet), persisting `include_in_prompt` toggles; `FactCategory` unified on the backend enum (`train\|fuel\|health\|life`, mock seed remapped). Fake-LLM `[fake-facts:…]` sentinel → LLM-free e2e ITs incl. the full async pipeline (Awaitility); `AbstractIntegrationTest` now drains leftover async work between tests. Gates: BE 573 green, FE 876/876 both modes. Plan: [`companion-v12-fact-extraction`](../superpowers/plans/2026-07-03-companion-v12-fact-extraction.md). Next: V1.3 never-ask-twice + advisor chain. |
| 2026-07-03 | **Companion V1.1 — knowledge_fact / learned_fact + prompt injection (`mezo-fnnq.6`)** shipped: the L3 memory spine — **v1 „megjegyez" started**. Two new owned tables: `knowledge_fact` (fact_text, category `train\|fuel\|health\|life`, source `chat\|pattern\|manual`, reinforcement_count, `include_in_prompt`, last_reinforced_at; injection index on `(created_by, include_in_prompt, reinforcement_count desc)`) + `learned_fact` (candidate → decision `accept\|reject\|refine` NULL-until-decided → promoted_fact_id; loose UUID refs `ON DELETE SET NULL`; **table-only** — the extraction/confirm flow is V1.2). Contract: `GET/POST /api/companion/fact` + `PATCH .../fact/{id}` (partial update; the app's **first PATCH** — `patchForBody` added to `ApiIntegrationTest`); manual add ships now (source=`manual`) so facts exist before extraction. Injection: `KnowledgeFactService.renderPromptBlock` — top-N (`mezo.companion.facts.top-n`: 10) prompt-included facts by reinforcement desc/newest, HU category labels, `""` when empty — inserted snapshot → **facts** → history in BOTH the sync and streamed turn. 18 new/extended IT cases (`KnowledgeFactServiceIT`, `LearnedFactPersistenceIT`, `CompanionFactApiIT`, ChatServiceIT injection-order proof via the fake echo, switch-off 404, properties binding); populators + TRUNCATE growth rules followed. Gates: BE 551 green; FE untouched (types regenerated only). Next: V1.2 extraction + confirm UI (`mezo-fnnq.7`). |
| 2026-07-03 | **Companion V0.5 — Tool calling + tool-chips (`mezo-fnnq.5`)** shipped: **v0 „lát engem" is complete** — history/aggregate questions get grounded answers and the FE chips stop being theater. 8 read-only tools in `feature/companion/tools/` over EXISTING reads (`get_recent_workouts`, `get_sport_sessions` incl. runs, `get_weight_trend`, `get_recent_meals` day rollups, `get_sleep`, `get_protocol_adherence`, `get_goal_progress`, `get_reta_cycle`), ownership-scoped via Spring AI `ToolContext` (userId from JWT, never model args), compact HU text results with `nincs adat` absences + config-clamped windows (`mezo.companion.tools.*`: call cap 6/turn, refs cap 10, day/week clamps). `CompanionToolRegistry` wraps every callback in `RecordingToolCallback` — audit ({type:'read',name,args}) + budget soft-fail + exception shielding are structurally unbypassable; tools contribute refs; both sync + streamed turns persist the audit into the (V0.2-typed, so-far-null) `ai_message.tool_calls`/`refs` envelopes; the wire chips carry `name(args)` (`get_sleep(days=3)`). IDENT-2 is now an ArchUnit rule (no HTTP/mail deps in the tools package). Port: `CompanionLlm` grew tools+toolContext params (NO message-list variant — Spring AI runs the tool loop in-adapter); the fake executes `[fake-tool:…]` sentinels through the REAL callbacks (LLM-free e2e ITs). 3 new plain finders in owning features (sleep/intake since-date, workout instances-between) + shared `GoalPrescriptionJson.currentSegment`. FE: zero code changes (V0.4 pass-through) — MSW `done` fixture + chip render asserts. Gates: BE 529 green, FE 856/856 both modes. Plan: [`companion-v05-tools`](../superpowers/plans/2026-07-03-companion-v05-tools.md). Next: V1.1 facts / V2.1 pgvector / V3.1 patterns (parallel-friendly). |
| 2026-07-03 | **Companion V0.4 — SSE streaming + FE ChatPage real (`mezo-fnnq.4`)** shipped: the chat is **usable on the phone**. Backend: `POST /api/companion/conversation/{id}/message/stream` (`text/event-stream`, events `delta` → `done`\|`error`, every data line JSON) — a **hand-written** `CompanionStreamController` + `ChatStreamService` over the port's `stream()`, with the turn split into two transactions (`ChatService.prepareTurn` user-row / `completeTurn` assistant-row; mid-stream failure ⇒ `error` event, user row kept, partial answers never persisted; the sync endpoint keeps its V0.2 single-tx semantics). **Contract precedent set + recorded** (`_platform-api-backend.md` §9): the SSE operation lives in the fragment under a dedicated `CompanionStream` tag whose generated interface is deliberately unimplemented; ArchUnit carries two documented allowlist entries. FE: `apiSse` fetch-ReadableStream client (`_client/api.ts`), `chatApi.ts` + `chatHooks.ts` (`useChat` bootstrap dual-read `{conversationId, messages, degraded, mode}`; `useChatActions` optimistic streamed-turn state machine), ChatPage rewritten real dual-mode (history, streamed draft bubble, honest header, IDENT-3 degraded banner on switch-off 404, disabled composer). k3s deploy sets `MEZO_FEATURE_COMPANION_ENABLED=false` until a real `GEMINI_API_KEY` secret lands. Tests: `ChatStreamServiceIT` + `CompanionStreamApiIT` + switch-off stream 404 (BE 496 green), `api.sse`/`chatApi`/`chatHooks`/`ChatPage` both-mode FE suites (856/856 both modes); live-verified with the fake echo AND a real Gemini streamed answer grounded in the V0.3 snapshot. Plan: [`companion-v04-sse-fe-chat`](../superpowers/plans/2026-07-03-companion-v04-sse-fe-chat.md). Next: V0.5 tool calling. |
| 2026-07-03 | **Companion V0.3 — ContextSnapshotAssembler (`mezo-fnnq.3`)** shipped: the **pain-killer** — every chat turn's system prompt now carries a deterministic, LLM-free `AKTUÁLIS ÁLLAPOT` block composed read-only from six other features: `[Profil]` (profile + `WeightTrendService` trend, no-fabrication guard on `dataSufficiency=NONE`), `[Cél]` (active goal + derived current week + prescription segment + `mealsPerDay`/`wakeTime`/`bedTime`), `[Edzés]` (active meso with DERIVED week, gym/sport weekly rhythm, last-7d gym/sport/run digest), `[Mai üzemanyag]` (FuelDay consumed/targets + protocol + intake count), `[Gyógyszer]` (retaDay + phase, honest zero when doseless), `[Regeneráció]` (latest sleep + check-in, note capped at 200 chars). Every absence renders `nincs adat`; strictly one-way coupling (companion → others, ArchUnit-guarded); config `mezo.companion.snapshot.{digest-days:7, checkin-note-max-chars:200}`. Four new derived repo finders (sleep/check-in latest, sport/run since-date) + `CheckInPopulator`. 10-test `ContextSnapshotAssemblerIT` + prompt-order proof in `ChatServiceIT` via the fake echo. Plan: [`companion-v03-context-snapshot`](../superpowers/plans/2026-07-03-companion-v03-context-snapshot.md). Next: V0.4 (SSE + FE ChatPage real). |
| 2026-07-03 | **Companion V0.2 — Conversations + sync chat endpoint (`mezo-fnnq.2`)** shipped: the Phase-3 chat **persistence spine**. Two owned tables (`ai_conversation` + `ai_message`, UUID/`created_by`/soft-delete, `tool_calls`/`refs` typed jsonb envelopes left null until V0.5), a contract fragment `api/feature/companion/companion.yml` (`GET/POST /api/companion/conversation`, `GET .../messages`, `POST .../message` — **sync JSON** answer, SSE deferred to V0.4), and two switch-gated services: `ConversationService` (CRUD spine) + `ChatService` (static HU companion-voice `SYSTEM_PROMPT` + last-20-message history windowing rendered into the prompt → one `CompanionLlm.complete()` → persists both turns; auto-title = first user message truncated to 80). Config `mezo.companion.chat.{history-window:20, title-max-chars:80}`. Every companion bean stays behind `mezo.feature.companion.enabled` (switch off ⇒ the whole `/api/companion/*` surface 404s). 5 new ITs (jsonb round-trip, conversation/chat services, HTTP flow, switch-off) against the `companion-fake` echo LLM — network never touched. FE untouched (mock ChatPage stays until V0.4). Living doc [`docs/features/companion.md`](../features/companion.md) born. |
| 2026-07-03 | **Companion V0.1 — Spring AI + provider ADR + skeleton (`mezo-fnnq.1`)** shipped: [ADR 0008](../decisions/0008-companion-llm-spring-ai-2-gemini.md) picks **Spring AI 2.0.0** (the Boot-4 line) + **Google Gemini** (`spring-ai-starter-model-google-genai`; tiers `gemini-2.5-flash`/`-pro` as config) + `GEMINI_API_KEY` env/SealedSecret delivery. `feature/companion` package born: `CompanionLlm` port + `GeminiCompanionLlm` real adapter + `FakeCompanionLlm` (`companion-fake` profile, tests never touch the network), all behind `mezo.feature.companion.enabled`; `CompanionProperties` (`mezo.companion.llm.*`). Proofs: 454 ITs green **without** an API key (dummy-default boot), real Gemini streamed hello via the `companion-smoke` profile runner. Unblocks V0.2 (chat spine) and V3.1 (patterns). |
| 2026-07-03 | **Phase 3 (AI brain) mapped**: the old Supabase-era PRD+architecture (`docs/old docs/`) surveyed and distilled into a current-stack design spec (`2026-07-03-phase3-companion-chat-design.md`) + a 14-slice companion roadmap (`2026-07-03-companion-roadmap.md`, epic `mezo-fnnq`, session-per-slice handoff). Key calls: deterministic context snapshot (not RAG) is the chat's backbone; RAG = episodic layer over narrative units; facts via confirm-promote pipeline; old Phase-7 motivation system explicitly out (XP-vs-narrative tension parked for a future ADR). Fuel P8 umbrella (`mezo-0h6w`) note-linked. |
| 2026-06-13 | Deploy direction decided: self-managed k3s + ArgoCD + pgAdmin (learning track). ADR 0001 + infra doc written. `docs/` decision/infra/milestone structure established. |
| 2026-06-14 | Deploy steps 0-2 done: Hetzner VPS hardened + Tailscale, k3s installed, full mezo (Postgres + backend + frontend) live on k8s at `https://46.225.112.172.sslip.io/` with Let's Encrypt HTTPS. `k8s/` manifests + Dockerfiles committed. Pending: pgAdmin (step 3), ArgoCD/GitOps (step 4). |
| 2026-06-24 | Slice C (Fuel) core shipped: Pantry (`pantry_item`, mezo-9xu) + 146-item catalog (mezo-zza), Recipes (`recipe`/`recipe_ingredient`, mezo-lns), Meal-logging (`meal`/`meal_item` + `/api/fuel/day/{date}`, mezo-arb) — backends + dual-mode FE, green both modes. Remaining Fuel (Stack/Protocol, weekly-plan, meal-scoring) Phase-3-deferred. |
| 2026-06-25 | Progression P1 (domain foundation) — curve, 3 tables (`skill_progress`/`level_up_event`/`perk_unlock`), entities/repos, perk catalog; triggers/HTTP deferred to P2 (`mezo-8e4`). |
| 2026-06-26 | Fuel polish (mezo-ksh/dh6/8xy/cki) + stock UI parked behind a flag (mezo-6nu). **Fuel completion roadmap** authored (`fuel-completion-roadmap.md`, epic `mezo-6r1`, phases P0–P8, dependency-ordered + deterministic-first); duplicate epic mezo-4ag closed. |
| 2026-06-26 | Fuel **P3 — Medication / Gyógyszer (mezo-d94)** shipped: `medication` + `medication_dose` tables as a **first-class** domain (NOT a pantry row), `/api/medication` + `…/{id}/dose` endpoints, server-derived 7-day Reta cycle (`MedicationCycleService`, retaDay from the dose log), the `/fuel/gyogyszer` tab + `LogDoseSheet`, and the **derived `retaDay` broadcast** via `useTodayScenario()` (every Reta surface reads it). Backend + dual-mode FE, green both modes. Supersedes the spec's earlier "Reta in pantry" P0b idea. |
| 2026-06-27 | Progression **P3b — SPORT path** (`mezo-lmox`, child of `mezo-8e4`) shipped: `sport_session` generalized into a 3-kind modality (the existing `sport` column became the `volleyball\|cross\|trx` discriminator via `ck_sport_session_sport`, + a `rounds` effort column), `ProgressionService.applySport` granting per-kind athletic XP, and a switch-gated `levelUp` on `SportSessionResponse` (mirrors gym finish + run log). Two baton refactors rode along: the streak **robustness rate lifted out of `gym()` into a shared `mezo.progression.robustness` node**, and `RunSignalCalculator` hardened to an ownership-scoped log load. Backend + dual-mode FE green (657/657 FE both modes). FE cross/TRX log-sheet + `LevelUpScreen` deferred to P5. Also fixed a time-of-day-flaky medication test (`mezo-yc9z`). |
| 2026-06-29 | Progression **P4 — profile endpoint** (`mezo-x71h`, child of `mezo-8e4`) shipped: `GET /api/progression/profile` — the **first** `api/feature/progression/` contract fragment and the **first** switch-gated controller (`ProgressionController` `@ConditionalOnProperty(PROGRESSION_SWITCH)`). `ProgressionService.getProfile` aggregates `skill_progress` over the fixed taxonomy: athlete-level (mean of the 11 non-robustness athletic, missing→level 1, `null`=ghost), 6 fixed radar axes (Erő blends the muscle-level mean via `mezo.progression.radar.strength-muscle-blend`), streak, and best-athletic/best-muscle highlights. Backend green (409 tests); FE green (regen only — cards + dual-mode mock are **P6**). |
| 2026-06-29 | Progression **P5 — FE level-up overlay** (`mezo-te8k`, child of `mezo-8e4`) shipped — **frontend-only**: the `levelUp` already on all three finish responses is now **captured** (gym `finishWorkout` + sport/run log mutations forward the response) and presented via a **single host** (`LevelUpProvider`/`useLevelUp`, mounted in `AppLayout`) — a full-bleed `LevelUpScreen` portal overlay over the chrome (hand-rolled CSS keyframes, rAF count-up, **`prefers-reduced-motion` first-class** via the new shared `useReducedMotion`; scales to multiple level-ups; no-level-up case never dead-ends). Gym layers it over the `WorkoutComplete` recap (revealed on `Tovább`) and the recap's PR framing now uses the real `max_strength` level-up signal. Also shipped the **cross/TRX kind selector** in `SportLogSheet` (de-hardcoded volleyball → `sport: volleyball\|cross\|trx` + `rounds`) and the deferred-close sheet contract (`onSave(input, done)`). Mock mode returns seeded `LevelUpResult` fixtures. FE green both modes (685/685) + build; visual verified. Profile cards (radar/muscle) remain **P6**. |
| 2026-07-02 | Fuel **P1 — Water logging (mezo-0z5)** shipped: a `water_log` table + `POST/DELETE /api/water-log` (on `MealController`), and `FuelDayService.consumed.water` is now a real per-day Σ (was always `= targets.water`, a fake 100% ring). FE: dual-mode `useWaterActions.logWater` + `+250/+500 ml` chips on `MacroHero`. Goal/Reta-aware water targets stay deferred (config constant). Backend + dual-mode FE green. |
| 2026-07-02 | Fuel **P2 — Stack / Protocol (mezo-09g)** shipped: the **first Fuel-owned backend package** (`backend/.../feature/fuel` + new `api/feature/fuel/fuel.yml`) — `protocol` + `protocol_item` + `supplement_intake` tables, `ProtocolService` (activate = supersede + `v+1`, confidence from `mezo.fuel.protocol.default-confidence` config) + `IntakeService`, `FuelController` (`GET/POST /api/fuel/protocol` + `GET/POST /api/fuel/intake` + `DELETE …/intake/entry/{id}`). FE: dual-mode `useStack`/`useProtocol` + `useStackActions`/`useProtocolActions` (`data/fuel/fuelApi.ts` + `stackHooks.ts`); Stack "Bekapcsolás · ma" is a **real** activation + slot tap-to-log intakes; Mai protocol-meta reads the real protocol (hidden at v0). The protocol persists **selection + version metadata only** (`buildProtocol` slots stay FE-computed); "Mentés protokollként" deferred; `useStackRecommendations` stays `[]` in real mode until P8. Folded in **mezo-4nu #1** (Stack `/api/goals` decouple via `useStackContext`). Backend + dual-mode FE green. |
| 2026-07-03 | Fuel **P5 — Fuel-Timeline / Mai day-planner (mezo-9ys)** shipped: the hand-authored Mai pacing timeline + Today's preview are now a **real client-composed day-planner** — a pure `buildDayPlan` (`features/fuel/logic/buildDayPlan.ts`) merges planned meal/snack windows (spread wake→kitchen-close, snapped around the day's REAL gym/sport/run blocks pre −75m / post +45m) + per-slot budgets from the goal `prescription` current-week segment (`deriveDailyBudget`, config `FuelDay.targets` fallback) + ±20% recipe-fit suggestions (`|Δkcal|→starred→|Δp|`) + the anchor-aware supplement protocol/intakes. Dual-mode `useFuelTimeline`/`useFuelPreview` (mock seed byte-parity / real composition). **Only backend work: three day-planner settings on the goal** (`mealsPerDay`/`wakeTime`/`bedTime` — the **FE's first goal write-back** via `EditGoalSheet` "Napi ritmus" → full-replace `goalResponseToUpsert` PUT). Client-side merge, no new Fuel endpoint. Sleep-derived anchors + rebalancing + prose → P7/P8. Backend + dual-mode FE green. |
| 2026-06-29 | Progression **P6 — FE profile cards** (`mezo-xje5`, child of `mezo-8e4`) shipped — **frontend-only**, and **completes the `mezo-8e4` epic** (P1 domain → P2 gym → P3a run → P3b sport → P4 profile API → P5 level-up overlay → P6 profile cards). A dual-mode `useProgressionProfile()` (`useDualQuery`: seeded `progressionProfileMock` / real `GET /api/progression/profile` / ghost `realEmpty` on switch-off 404) feeds two cards below `BiometricCard` in `ProfileView`: **`AthleticRadarCard`** (hand-rolled SVG hexagon radar of the 6 server-computed `radarAxes` via the pure `radarGeometry` helper + athlete-level/best/streak stats) and **`MuscleLevelsCard`** (top-N by level + reserve note). Both ghost on `athleteLevel == null` (mirrors BiometricCard) and reuse the P5 `useReducedMotion`; "Teljes profil"/"Mind a 13" links are inert v1 stubs. New `progressionApi.ts` GET client (no contract regen — P4 types already generated). FE green both modes + build; browser-verified. |
