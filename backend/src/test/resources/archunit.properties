# FreezingArchRule store (ArchitectureTest.feature_slices_are_cycle_free):
# known pre-existing violations live in src/test/resources/archunit-store and are
# tolerated; new violations fail. Delete entries (and commit) as cycles get fixed.
freeze.store.default.path=src/test/resources/archunit-store
freeze.store.default.allowStoreCreation=true
